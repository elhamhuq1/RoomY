---
phase: 10-onboarding-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - app/(auth)/sign-up.tsx
  - app/(auth)/sign-in.tsx
  - app/(auth)/forgot-password.tsx
autonomous: true
requirements:
  - ONBD-02

must_haves:
  truths:
    - "Sign-up screen has cream background, small glassmorphism logo, and inset-style form inputs"
    - "Sign-in screen mirrors sign-up layout with same cream bg, logo, and input styling"
    - "Social auth buttons render as full-width branded pills (Google: white bg + colored G, Apple: black bg + white logo)"
    - "All user-facing 'Sign In' text is renamed to 'Log In' across auth and forgot-password screens"
    - "Form inputs show light gray fill (#F5F5F5) by default and white bg with brand border on focus"
  artifacts:
    - path: "app/(auth)/sign-up.tsx"
      provides: "Restyled sign-up with cream bg, glassmorphism logo, inset inputs, branded social buttons"
      min_lines: 200
    - path: "app/(auth)/sign-in.tsx"
      provides: "Restyled sign-in (now 'Log In') mirroring sign-up layout"
      min_lines: 200
    - path: "app/(auth)/forgot-password.tsx"
      provides: "Updated 'Back to Log In' text and cream bg"
      min_lines: 100
  key_links:
    - from: "app/(auth)/sign-up.tsx"
      to: "expo-blur"
      via: "BlurView for small glassmorphism logo"
      pattern: "BlurView"
    - from: "app/(auth)/sign-up.tsx"
      to: "lib/onboarding-images.ts"
      via: "import ONBOARDING_CREAM"
      pattern: "ONBOARDING_CREAM"
---

<objective>
Restyle the sign-up, sign-in (now "Log In"), and forgot-password auth screens with cream backgrounds, small glassmorphism logos, inset-style form inputs, branded social auth buttons, and the "Sign In" to "Log In" text rename.

Purpose: Delivers a cohesive, polished auth experience that matches the welcome carousel's visual language.
Output: 3 restyled auth screen files with consistent cream + glassmorphism + inset input styling
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-onboarding-flow/10-CONTEXT.md
@.planning/phases/10-onboarding-flow/10-RESEARCH.md
@.planning/phases/10-onboarding-flow/10-01-SUMMARY.md
@app/(auth)/sign-up.tsx
@app/(auth)/sign-in.tsx
@app/(auth)/forgot-password.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle sign-up and sign-in screens with cream bg, glassmorphism, inset inputs, and social buttons</name>
  <files>
    app/(auth)/sign-up.tsx
    app/(auth)/sign-in.tsx
  </files>
  <action>
    Restyle both screens to mirror each other in layout (per CONTEXT.md: "same structure, only form fields and CTA text differ").

    **For both sign-up.tsx and sign-in.tsx:**

    1. **Background:** Set screen bg to ONBOARDING_CREAM (import from lib/onboarding-images.ts).

    2. **Small glassmorphism logo container** at top center:
       - 56x56px (smaller than welcome's 80x80), borderRadius 14
       - BlurView intensity={25} tint="light" with overflow hidden
       - Inner View with semi-transparent white bg (Platform.select: iOS rgba(255,255,255,0.3), Android rgba(255,255,255,0.7))
       - RoomY logo: `require('@/assets/icon.png')`, 36x36px, resizeMode="contain"
       - Centered at top with some top margin below safe area

    3. **Screen title + subtitle** below logo:
       - sign-up.tsx: "Create Account" title, "Start managing your household" subtitle
       - sign-in.tsx: "Welcome Back" title, "Log in to your account" subtitle (NOTE: "Log in" not "Sign in")
       - Title: large bold text, centered
       - Subtitle: body text, neutral.secondary color, centered

    4. **Form inputs** with inset style:
       - Default state: bg-[#F5F5F5], no border, rounded-2xl, px-4 py-3.5
       - Focused state: bg-white, border-2 border-brand (#10B981), rounded-2xl
       - Error state: bg-white, border-2 border-semantic-error (red)
       - Track focus state per input with useState
       - sign-up.tsx fields: Email, Password, Confirm Password
       - sign-in.tsx fields: Email, Password
       - Error messages below inputs in red text (keep existing validation logic)

    5. **Primary CTA button:**
       - Full-width, brand bg (#10B981), white text, rounded-2xl, py-3.5
       - sign-up.tsx: "Create Account"
       - sign-in.tsx: "Log In" (NOT "Sign In")
       - Keep existing loading/disabled states

    6. **Social auth buttons** below the primary CTA, separated by an "or" divider:
       - "or" text with horizontal lines on each side
       - Google button: full-width pill, white bg, gray border, colored Google "G" logo (use Ionicons logo-google), text "Continue with Google"
       - Apple button (iOS only): full-width pill, black bg, white Apple logo (Ionicons logo-apple), white text "Continue with Apple"
       - Both buttons rounded-2xl, py-3.5
       - Keep existing onPress handlers for social auth

    7. **Bottom link:**
       - sign-up.tsx: "Already have an account? Log in" -> navigates to sign-in
       - sign-in.tsx: "Don't have an account? Sign up" -> navigates to sign-up
       - Keep "Forgot password?" link on sign-in screen

    **Preserve ALL existing auth logic, Supabase calls, error handling, loading states, and navigation.** Only change visual presentation. Keep KeyboardAvoidingView + ScrollView pattern.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
    <manual>Both auth screens show cream bg, small glassmorphism logo, inset inputs with focus states, branded social buttons</manual>
  </verify>
  <done>Sign-up and sign-in screens have cream bg, small glassmorphism logo, inset-style inputs (gray fill default, white+brand border on focus), branded social auth pills, and "Log In" text on sign-in</done>
</task>

<task type="auto">
  <name>Task 2: Restyle forgot-password and complete "Sign In" to "Log In" rename across all auth files</name>
  <files>
    app/(auth)/forgot-password.tsx
    app/(auth)/sign-up.tsx
    app/(auth)/sign-in.tsx
  </files>
  <action>
    1. **Restyle forgot-password.tsx:**
       - Set bg to ONBOARDING_CREAM
       - Small glassmorphism logo at top (same pattern as sign-up/sign-in: 56x56, BlurView)
       - Title: "Reset Password"
       - Subtitle: "Enter your email and we'll send you a reset link"
       - Email input with inset style (same as auth screens)
       - Primary CTA: "Send Reset Link" (brand button, same style)
       - "Back to Log In" link at bottom (rename from "Back to Sign In")
       - Keep existing success state and email sending logic
       - In success state, update any "sign in" text to "log in"

    2. **Verify "Sign In" -> "Log In" rename is complete across ALL auth files:**
       Grep for remaining "Sign In", "Sign in", "sign in" text in display strings (not file names or code identifiers). Known locations from research:
       - `app/(auth)/welcome.tsx` line ~125: "Sign in" link text -> should be "Log in" (done in Plan 01)
       - `app/(auth)/sign-in.tsx` lines ~122, ~208: screen subtitle and button -> verify "Log In"
       - `app/(auth)/sign-up.tsx` line ~275: "Sign in" link text -> verify "Log in"
       - `app/(auth)/forgot-password.tsx` line ~75: "Back to Sign In" -> "Back to Log In"

       The file NAME `sign-in.tsx` stays unchanged (it's a route, not user-facing text).
       Do NOT change code variable names or function names, only user-visible display text.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && grep -rn "Sign [Ii]n" app/\(auth\)/*.tsx --include="*.tsx" | grep -v "signIn\|sign-in\|SignIn" | head -20 && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
    <manual>Forgot-password has cream bg and glassmorphism logo. No user-visible "Sign In" text remains in auth screens.</manual>
  </verify>
  <done>Forgot-password screen has cream bg, glassmorphism logo, inset input, and "Back to Log In" text. All user-facing "Sign In" text renamed to "Log In" across welcome, sign-up, sign-in, and forgot-password screens.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 3 auth screens (sign-up, sign-in, forgot-password) render with cream bg, glassmorphism logo, inset inputs
- `grep -rn "Sign [Ii]n" app/(auth)/*.tsx` returns no user-visible display text matches (only code identifiers)
- Social auth buttons styled as branded pills (Google white, Apple black)
</verification>

<success_criteria>
- Sign-up and sign-in screens mirror each other with cream bg, small glassmorphism logo, inset inputs, branded social buttons
- Forgot-password screen matches with cream bg and glassmorphism
- All user-visible "Sign In" text replaced with "Log In"
- All existing auth logic, validation, and navigation preserved
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/10-onboarding-flow/10-02-SUMMARY.md`
</output>
