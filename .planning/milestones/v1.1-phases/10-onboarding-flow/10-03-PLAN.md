---
phase: 10-onboarding-flow
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - app/(onboarding)/profile.tsx
  - app/(onboarding)/household-choice.tsx
  - app/(onboarding)/create-household.tsx
autonomous: true
requirements:
  - ONBD-03
  - ONBD-04
  - ONBD-05

must_haves:
  truths:
    - "Profile screen shows a live Avatar component that updates its initial letter as the user types their display name"
    - "Profile screen displays the display-name.jpg illustration as a hero image"
    - "Household choice screen presents create/join options as large cards with gradient icon containers"
    - "Household choice screen displays the setup-home.jpg illustration"
    - "Create household name input screen shows name-household.jpg illustration and branded house icon"
    - "All three screens show StepProgressBar with correct step filled (profile=0, choice=1, create=1)"
    - "All three screens use cream background"
  artifacts:
    - path: "app/(onboarding)/profile.tsx"
      provides: "Profile screen with live Avatar preview, illustration, progress bar, inset input"
      min_lines: 100
    - path: "app/(onboarding)/household-choice.tsx"
      provides: "Household choice with gradient icon cards, illustration, progress bar"
      min_lines: 60
    - path: "app/(onboarding)/create-household.tsx"
      provides: "Create household name form with illustration, progress bar, inset input (celebration state unchanged)"
      min_lines: 200
  key_links:
    - from: "app/(onboarding)/profile.tsx"
      to: "components/ui/Avatar.tsx"
      via: "Avatar component with live initial"
      pattern: "Avatar"
    - from: "app/(onboarding)/profile.tsx"
      to: "components/ui/StepProgressBar.tsx"
      via: "StepProgressBar currentStep={0}"
      pattern: "StepProgressBar"
    - from: "app/(onboarding)/household-choice.tsx"
      to: "expo-linear-gradient"
      via: "LinearGradient inside icon containers for create/join cards"
      pattern: "LinearGradient"
    - from: "app/(onboarding)/create-household.tsx"
      to: "components/ui/StepProgressBar.tsx"
      via: "StepProgressBar currentStep={1}"
      pattern: "StepProgressBar"
---

<objective>
Restyle the profile, household choice, and create-household name form screens with illustrations, cream backgrounds, StepProgressBar integration, live Avatar preview, and gradient icon cards.

Purpose: Delivers the core onboarding wizard visual experience from profile setup through household creation, with progress indication.
Output: 3 restyled onboarding screen files with consistent cream + illustration + progress bar styling
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-onboarding-flow/10-CONTEXT.md
@.planning/phases/10-onboarding-flow/10-RESEARCH.md
@.planning/phases/10-onboarding-flow/10-01-SUMMARY.md
@app/(onboarding)/profile.tsx
@app/(onboarding)/household-choice.tsx
@app/(onboarding)/create-household.tsx
@components/ui/StepProgressBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle profile screen with live Avatar preview, illustration, and progress bar</name>
  <files>app/(onboarding)/profile.tsx</files>
  <action>
    Restyle `app/(onboarding)/profile.tsx` to match CONTEXT.md decisions:

    1. **Background:** Set to ONBOARDING_CREAM (import from lib/onboarding-images.ts).

    2. **StepProgressBar** at top of screen:
       - `<StepProgressBar currentStep={0} totalSteps={3} showBack={false} />`
       - No back button on profile screen (it's the first onboarding screen after auth)
       - Position below SafeAreaView top padding

    3. **Illustration hero:**
       - Display `ONBOARDING_IMAGES.displayName` (display-name.jpg) as hero image
       - Image takes ~40% of available height, resizeMode="contain", centered
       - Use `Image` from react-native (not expo-image)

    4. **Title + subtitle:**
       - "What's your name?" title (large bold, centered)
       - "This is how your roommates will see you" subtitle (body, neutral.secondary, centered)

    5. **Live Avatar preview:**
       - Import Avatar from components/ui
       - Render Avatar component (size "xl" or "lg") centered above or alongside the input
       - Avatar shows the first letter of the displayName as the user types
       - When displayName is empty, show a placeholder initial (e.g., "?" or empty)
       - Use a temporary/deterministic user ID string for gradient color (e.g., "preview" or the actual session user ID if available)

    6. **Display name input:**
       - Inset style: bg-[#F5F5F5] default, bg-white + border-2 border-brand on focus (same pattern as auth screens)
       - Rounded-2xl, px-4, py-3.5
       - Placeholder: "Display name"
       - Track focus state with useState

    7. **Continue button:**
       - Full-width brand button at bottom: "Continue"
       - Disabled when name is empty
       - Keep existing Supabase profile update logic and navigation

    **Preserve ALL existing logic** for profile saving, error handling, and navigation to household-choice.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
    <manual>Profile screen shows cream bg, progress bar (segment 1 filling), display-name.jpg illustration, live Avatar that updates initial as you type, inset input</manual>
  </verify>
  <done>Profile screen has cream bg, StepProgressBar at step 0, display-name.jpg illustration, live Avatar preview that updates initial letter as user types, inset-style display name input, and Continue button</done>
</task>

<task type="auto">
  <name>Task 2: Restyle household-choice and create-household name form with gradient cards and illustrations</name>
  <files>
    app/(onboarding)/household-choice.tsx
    app/(onboarding)/create-household.tsx
  </files>
  <action>
    **Restyle household-choice.tsx:**

    1. **Background:** ONBOARDING_CREAM.

    2. **StepProgressBar** at top:
       - `<StepProgressBar currentStep={1} totalSteps={3} onBack={() => router.back()} />`
       - Back button visible, navigates back to profile

    3. **Illustration hero:**
       - Display `ONBOARDING_IMAGES.setupHome` (setup-home.jpg)
       - ~40% height, resizeMode="contain", centered

    4. **Title + subtitle:**
       - "Set Up Your Home" title (large bold, centered)
       - "Create a new household or join an existing one" subtitle (body, neutral.secondary, centered)

    5. **Two large option cards** (create and join):
       - Use Card component from components/ui for each option
       - Each card contains:
         - Gradient icon container at top: Use LinearGradient from expo-linear-gradient inside a 48x48 rounded container
           - Create card: house icon (Ionicons "home"), green gradient (brand colors)
           - Join card: key icon (Ionicons "key"), blue/violet gradient
         - Title below icon: "Create Household" / "Join Household"
         - Description: "Start a new home and invite roommates" / "Enter an invite code from your roommate"
       - Cards should be full-width, stacked vertically with gap
       - Each card is a Pressable navigating to create-household / join-household
       - Keep existing navigation logic

    **Restyle create-household.tsx (NAME FORM STEP ONLY):**

    Only restyle the initial name input form. Do NOT modify the invite code success/celebration state (that's Plan 04).

    1. **Background:** ONBOARDING_CREAM.

    2. **StepProgressBar** at top:
       - `<StepProgressBar currentStep={1} totalSteps={3} onBack={() => router.back()} />`

    3. **Illustration hero:**
       - Display `ONBOARDING_IMAGES.nameHousehold` (name-household.jpg)
       - ~35-40% height, resizeMode="contain", centered

    4. **Branded house icon:**
       - An IconContainer (from components/ui) with "home" Ionicons icon in brand variant, centered above the title

    5. **Title + subtitle:**
       - "Name Your Household" title
       - "Give your home a name your roommates will recognize" subtitle

    6. **Household name input:**
       - Inset style (same as auth/profile: bg-[#F5F5F5] default, white+brand border on focus)
       - Placeholder: "e.g., The Lake House"

    7. **Create button:**
       - Full-width brand button: "Create Household"
       - Disabled when name is empty
       - Keep existing household creation logic

    **Important:** The `create-household.tsx` file has TWO states: (1) the name form and (2) the invite code success view. Only restyle state (1) here. Leave the invite code success rendering logic untouched for Plan 04. You may restructure the conditional rendering to cleanly separate the two states if needed, but do not change the success state's visual output yet.

    **Preserve ALL existing logic** for household creation, error handling, and state transitions.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
    <manual>Household choice shows cream bg, setup-home.jpg, two gradient icon cards. Create household shows name-household.jpg, inset input, progress bar at step 2.</manual>
  </verify>
  <done>Household-choice screen has cream bg, StepProgressBar at step 1, setup-home.jpg illustration, two large cards with gradient icon containers (home/key icons). Create-household name form has cream bg, StepProgressBar at step 1, name-household.jpg, branded house icon, inset input. Invite code success state is visually unchanged.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Profile screen shows live Avatar preview, display-name.jpg illustration, StepProgressBar step 0
- Household choice shows setup-home.jpg, two gradient icon cards
- Create household name form shows name-household.jpg, inset input, StepProgressBar step 1
- Create household invite code success state still functions (visually unchanged)
</verification>

<success_criteria>
- Profile screen: cream bg, progress bar, illustration, live Avatar updating with typed name, inset input
- Household choice: cream bg, progress bar, illustration, two gradient icon cards navigating correctly
- Create household name form: cream bg, progress bar, illustration, branded house icon, inset input
- All existing logic (Supabase calls, navigation, validation) preserved
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/10-onboarding-flow/10-03-SUMMARY.md`
</output>
