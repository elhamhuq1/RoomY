---
phase: 10-onboarding-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/onboarding/split-expenses.jpg
  - assets/onboarding/shared-grocery.jpg
  - assets/onboarding/chore-rotation.jpg
  - assets/onboarding/display-name.jpg
  - assets/onboarding/setup-home.jpg
  - assets/onboarding/name-household.jpg
  - assets/onboarding/invite-code.jpg
  - lib/onboarding-images.ts
  - components/ui/StepProgressBar.tsx
  - components/ui/index.ts
  - app/(auth)/welcome.tsx
autonomous: true
requirements:
  - ONBD-01
  - ONBD-08

must_haves:
  truths:
    - "Welcome screen displays a 3-slide horizontal carousel with illustration hero images taking ~50% of each slide"
    - "Glassmorphism logo container with RoomY logo is pinned above the carousel and does not scroll with slides"
    - "Each carousel slide shows a title, subtitle, and emoji feature badge pills below the subtitle"
    - "Page indicator dots show active state as wide brand pill and inactive as small gray circles"
    - "StepProgressBar component renders 3 thin segmented bars with animated fill on the current segment"
    - "Onboarding images load from assets/onboarding/ via static require map"
  artifacts:
    - path: "assets/onboarding/split-expenses.jpg"
      provides: "Welcome slide 1 illustration"
    - path: "lib/onboarding-images.ts"
      provides: "Static require map for all 7 onboarding illustrations plus cream constant"
      exports: ["ONBOARDING_IMAGES", "ONBOARDING_CREAM"]
    - path: "components/ui/StepProgressBar.tsx"
      provides: "Instagram Stories-style 3-segment animated progress bar"
      min_lines: 40
    - path: "app/(auth)/welcome.tsx"
      provides: "Restyled welcome carousel with glassmorphism, illustrations, emoji badges"
      min_lines: 100
  key_links:
    - from: "app/(auth)/welcome.tsx"
      to: "lib/onboarding-images.ts"
      via: "import ONBOARDING_IMAGES"
      pattern: "ONBOARDING_IMAGES"
    - from: "app/(auth)/welcome.tsx"
      to: "expo-blur"
      via: "BlurView for glassmorphism logo"
      pattern: "BlurView"
    - from: "components/ui/StepProgressBar.tsx"
      to: "react-native-reanimated"
      via: "withTiming for segment fill animation"
      pattern: "withTiming"
---

<objective>
Set up onboarding infrastructure (images, constants, StepProgressBar component) and restyle the welcome carousel screen with glassmorphism logo, illustration hero slides, emoji feature badges, and page indicator dots.

Purpose: Establishes the shared assets and components all other onboarding plans depend on, and delivers the first screen users see -- the welcome carousel.
Output: 7 bundled images in assets/onboarding/, image map + cream constant in lib/, StepProgressBar component in components/ui/, fully restyled welcome.tsx
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-onboarding-flow/10-CONTEXT.md
@.planning/phases/10-onboarding-flow/10-RESEARCH.md
@app/(auth)/welcome.tsx
@components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up onboarding assets, image map, cream constant, and StepProgressBar component</name>
  <files>
    assets/onboarding/split-expenses.jpg
    assets/onboarding/shared-grocery.jpg
    assets/onboarding/chore-rotation.jpg
    assets/onboarding/display-name.jpg
    assets/onboarding/setup-home.jpg
    assets/onboarding/name-household.jpg
    assets/onboarding/invite-code.jpg
    lib/onboarding-images.ts
    components/ui/StepProgressBar.tsx
    components/ui/index.ts
  </files>
  <action>
    1. Install expo-blur and expo-clipboard:
       `npx expo install expo-blur expo-clipboard`

    2. Create `assets/onboarding/` directory and copy all 7 JPGs from `docs/onboarding-images/` into it. Keep originals in docs/ (they're reference material).

    3. Create `lib/onboarding-images.ts` with:
       - `ONBOARDING_CREAM = '#F5F0EB'` constant (matches illustration backgrounds per research)
       - `ONBOARDING_IMAGES` object with static `require()` calls for all 7 images:
         splitExpenses, sharedGrocery, choreRotation, displayName, setupHome, nameHousehold, inviteCode
       - All requires use `@/assets/onboarding/` paths
       - Do NOT use dynamic require (Metro limitation)

    4. Create `components/ui/StepProgressBar.tsx`:
       - Props: `currentStep: number` (0-indexed), `totalSteps: number` (default 3), `onBack?: () => void`, `showBack?: boolean` (default true)
       - Layout: Row with optional back button (Ionicons chevron-back, 24px) on left, segments filling remaining width
       - Each segment is a thin bar (height 3px, rounded) with 2px horizontal margin between segments
       - Previous segments: fully filled with brand color (#10B981)
       - Current segment: animated fill left-to-right using Reanimated withTiming (duration 400ms, Easing.out(Easing.ease))
       - Future segments: muted gray (#E2E8F0)
       - Use useSharedValue + useAnimatedStyle for the fill width animation
       - Trigger animation via useEffect when currentStep changes
       - Component height ~44px total (includes padding for touch target on back button)
       - Import Animated, useAnimatedStyle, withTiming, Easing, useSharedValue from react-native-reanimated

    5. Add StepProgressBar to `components/ui/index.ts` barrel export:
       `export { StepProgressBar } from './StepProgressBar';`
       Also export `StepProgressBarProps` type if defined.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && ls assets/onboarding/*.jpg | wc -l && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
    <manual>7 JPGs in assets/onboarding/, TypeScript compiles clean</manual>
  </verify>
  <done>7 images bundled in assets/onboarding/, lib/onboarding-images.ts exports ONBOARDING_IMAGES map and ONBOARDING_CREAM constant, StepProgressBar component exists with animated fill segments, expo-blur and expo-clipboard installed, barrel export updated</done>
</task>

<task type="auto">
  <name>Task 2: Restyle welcome.tsx with glassmorphism carousel and emoji badges</name>
  <files>app/(auth)/welcome.tsx</files>
  <action>
    Rewrite `app/(auth)/welcome.tsx` to match the CONTEXT.md decisions:

    **Background:** Set entire screen bg to ONBOARDING_CREAM (import from lib/onboarding-images.ts).

    **Fixed header (does NOT scroll with carousel):**
    - Glassmorphism logo container using expo-blur BlurView:
      - 80x80px, borderRadius 20, overflow hidden
      - BlurView intensity={25} tint="light"
      - Inner View with semi-transparent white bg (rgba(255,255,255,0.3) iOS, rgba(255,255,255,0.7) Android via Platform.select)
      - RoomY logo image inside: `require('@/assets/icon.png')`, 56x56, resizeMode="contain"
    - "RoomY" title text below logo (text-page-title or similar large bold)
    - Tagline subtitle below title (text-body, muted color)
    - Center-align the entire header block

    **Carousel (horizontal ScrollView with pagingEnabled):**
    - Keep existing ScrollView + pagingEnabled pattern from current welcome.tsx
    - 3 slides using VALUE_PROPS data (keep existing content structure):
      Slide 1: splitExpenses image, "Split Expenses Fairly", subtitle, badges: "💰 Fair splits", "⚡ One tap"
      Slide 2: sharedGrocery image, "Shared Grocery Lists", subtitle, badges: "🛒 Real-time sync", "📱 Auto-split costs"
      Slide 3: choreRotation image, "Chore Rotation", subtitle, badges: "🔄 Auto-rotate", "✅ Track streaks"
    - Each slide layout:
      - Image takes ~50% of slide height, resizeMode="contain", centered
      - Title below image (text-section-heading or text-card-title, bold, centered)
      - Subtitle below title (text-body, neutral.secondary color, centered, max 2 lines)
      - Emoji feature badges below subtitle: horizontal row of pill chips (rounded-full, bg-white/80, px-3 py-1.5, text-sm)

    **Page indicator dots:**
    - Below carousel
    - Active dot: wide pill shape (width 24px, height 8px, rounded-full, brand bg #10B981)
    - Inactive dots: small circle (width 8px, height 8px, rounded-full, gray bg #D1D5DB)
    - Track scroll position to update active index (onMomentumScrollEnd or onScroll with page calculation)

    **CTA section (below dots):**
    - "Get Started" primary button (use existing Button component or styled Pressable, full-width, brand bg, white text, rounded-2xl)
    - "Already have an account? Log in" text link below (note: "Log in" not "Sign in" per rename decision)
    - Both navigate to the same routes as current implementation

    **Preserve all existing navigation logic and imports.** Do NOT change any router.push/replace calls or auth flow. This is purely visual.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
    <manual>Open app in Expo Go, see welcome screen with cream bg, glassmorphism logo, 3-slide carousel with illustrations, emoji badges, and branded page dots</manual>
  </verify>
  <done>Welcome screen shows cream background, glassmorphism logo container pinned above carousel, 3 swipeable slides with illustration heroes (~50% height), titles, subtitles, and emoji feature badge pills, active/inactive page indicator dots, "Get Started" button and "Log in" link</done>
</task>

</tasks>

<verification>
- `ls assets/onboarding/*.jpg | wc -l` returns 7
- `npx tsc --noEmit` passes with zero errors
- StepProgressBar exported from components/ui barrel
- Welcome screen renders with carousel, glassmorphism logo, illustrations, emoji badges, page dots
</verification>

<success_criteria>
- All 7 onboarding images bundled in assets/onboarding/ and accessible via static require map
- StepProgressBar component renders 3 animated segments with back button support
- Welcome carousel shows glassmorphism logo, 3 illustration slides with emoji badges, branded page dots
- expo-blur and expo-clipboard installed
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/10-onboarding-flow/10-01-SUMMARY.md`
</output>
