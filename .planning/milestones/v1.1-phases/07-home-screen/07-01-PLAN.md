---
phase: 07-home-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/home/GreetingHeader.tsx
  - components/home/MembersCard.tsx
  - components/home/BalanceSummaryCard.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-04]

must_haves:
  truths:
    - "Home screen shows a greeting that changes based on time of day (morning/afternoon/evening) with the current date"
    - "Members card displays household name, avatar row for all members, and a working invite link"
    - "Balance summary card renders with dark gradient background showing the net dollar amount and action buttons"
  artifacts:
    - path: "components/home/GreetingHeader.tsx"
      provides: "Time-aware greeting with date display"
      min_lines: 20
    - path: "components/home/MembersCard.tsx"
      provides: "Household name, avatar row, invite copy/share"
      min_lines: 40
    - path: "components/home/BalanceSummaryCard.tsx"
      provides: "Dark gradient balance card with action buttons"
      min_lines: 50
  key_links:
    - from: "components/home/GreetingHeader.tsx"
      to: "date-fns"
      via: "format(new Date(), 'EEEE, MMMM d') for date display"
      pattern: "format.*new Date"
    - from: "components/home/MembersCard.tsx"
      to: "components/ui/Avatar.tsx"
      via: "Avatar component for each member"
      pattern: "<Avatar"
    - from: "components/home/BalanceSummaryCard.tsx"
      to: "expo-linear-gradient"
      via: "LinearGradient for dark background"
      pattern: "<LinearGradient"
    - from: "components/home/BalanceSummaryCard.tsx"
      to: "expo-router"
      via: "router.push for navigation on card tap and buttons"
      pattern: "router\\.push"
---

<objective>
Create the top-of-fold home screen section components: time-aware greeting header, members card with invite functionality, and dark gradient balance summary card.

Purpose: These three components form the hero section of the new dashboard -- visible without scrolling -- providing immediate context on greeting, household members, and financial position.
Output: Three standalone section components in `components/home/` ready for assembly in Plan 03.
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-home-screen/07-CONTEXT.md
@.planning/phases/07-home-screen/07-RESEARCH.md
@components/ui/Avatar.tsx
@components/ui/Card.tsx
@components/ui/Button.tsx
@components/ui/index.ts
@lib/theme/colors.ts
@lib/auth-context.tsx
@app/(app)/(tabs)/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GreetingHeader and MembersCard components</name>
  <files>components/home/GreetingHeader.tsx, components/home/MembersCard.tsx</files>
  <action>
Create `components/home/GreetingHeader.tsx`:
- Accept props: `{ userName: string }` (first name of current user)
- Compute greeting based on `new Date().getHours()`: <12 "Good morning", <17 "Good afternoon", else "Good evening"
- Render greeting as `text-page-title text-neutral-text` with comma and first name: "Good morning, Elham"
- Render current date below as `text-metadata text-neutral-secondary mt-1` using `format(new Date(), 'EEEE, MMMM d')` from date-fns
- No settings icon button needed (already in tab header via _layout.tsx)
- Wrap in `<View className="px-5 pt-4">`

Create `components/home/MembersCard.tsx`:
- Accept props: `{ members: { user_id: string; display_name: string }[]; householdName: string; inviteCode: string }`
- Use the `Card` component from `@/components/ui` as the outer wrapper
- Render household name as `text-overline text-neutral-secondary` at top
- Render a horizontal row of `Avatar` components (size="md") for each member, with first name label below each using `text-metadata text-neutral-secondary mt-1`, truncated to 1 line
- Below the avatar row, render an invite section:
  - "Invite" label with `text-metadata text-neutral-secondary`
  - The invite code displayed in a styled row with copy and share icon buttons
  - Copy uses `expo-clipboard` (`Clipboard.setStringAsync`), show "Copied!" feedback via local state with 2s timeout
  - Share uses React Native `Share.share({ message: 'Join my household on RoomY! Use code: ${inviteCode}' })`
- When only 1 member (solo creator), make the invite section more prominent -- larger code display, "Invite your roommates" prompt text

Pattern reference: Copy existing invite logic from current `index.tsx` (handleShare, handleCopyCode functions).
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Both files exist and compile without type errors</manual>
  </verify>
  <done>GreetingHeader renders time-appropriate greeting with formatted date. MembersCard renders household name, avatar row with Avatar components, and working copy/share invite code buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Create BalanceSummaryCard component</name>
  <files>components/home/BalanceSummaryCard.tsx</files>
  <action>
Create `components/home/BalanceSummaryCard.tsx`:
- Accept props: `{ netAmount: number; onSettleUp: () => void; onRequest: () => void; onCardPress: () => void }`
  - `netAmount`: current user's net position (positive = owed to you, negative = you owe)
- Use `Pressable` wrapping `LinearGradient` from `expo-linear-gradient` as the card container
  - Gradient colors: `['#1E293B', '#0F172A']` (slate-800 to slate-900) diagonal
  - `start={{ x: 0, y: 0 }} end={{ x: 1, y: 1 }}`
  - Apply `className="rounded-card-lg p-5"` on the gradient (use tailwind.config rounded-card-lg or fallback to `borderRadius: 16` inline)
- Top: `text-overline` label "YOUR BALANCE" in `text-neutral-tertiary` (light on dark bg -- use `style={{ color: '#94A3B8' }}` if NativeWind class conflicts with dark bg)
- Main amount display using `text-key-number` typography:
  - If netAmount > 0: "You're owed $X.XX" in green (`style={{ color: '#22C55E' }}` -- semantic success)
  - If netAmount < 0: "You owe $X.XX" in red (`style={{ color: '#EF4444' }}` -- semantic error). Show absolute value.
  - If netAmount === 0: "All settled up" in white with a checkmark icon
- Format dollar amount with 2 decimal places, `Math.abs(netAmount).toFixed(2)`
- Bottom row: Two buttons side by side using `flex-row gap-3 mt-4`
  - "Settle Up" button: Use `Button` component with `variant="primary"`, `onPress={onSettleUp}`
  - "Request" button: Use `Button` component with `variant="outline"`, `onPress={onRequest}`
  - Both buttons get `className="flex-1"` for equal width
  - When netAmount === 0 (all settled): buttons still visible but with `disabled={false}` and slightly muted (use opacity style)
- Outer Pressable `onPress={onCardPress}` navigates to expenses tab for full breakdown

Per user decision: Both buttons always visible regardless of owe direction.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>BalanceSummaryCard compiles, accepts correct props, uses LinearGradient with dark colors</manual>
  </verify>
  <done>BalanceSummaryCard renders dark gradient background with conditional owe/owed text color, dollar amount, and two action buttons. Card body navigates to expenses on press.</done>
</task>

</tasks>

<verification>
- All three component files exist under `components/home/`
- `npx tsc --noEmit` passes without errors
- GreetingHeader uses date-fns format and time-of-day logic
- MembersCard uses Avatar component from Phase 6
- BalanceSummaryCard uses LinearGradient with dark gradient colors
- No hardcoded color values -- uses colors.ts tokens or design system classes except for gradient hex values and semantic text colors on dark bg
</verification>

<success_criteria>
- Three section components created and type-check clean
- GreetingHeader shows time-appropriate greeting + formatted date
- MembersCard shows household name overline, avatar row, invite copy/share
- BalanceSummaryCard shows dark gradient with conditional owe/owed display and action buttons
- Components accept well-typed props (no `any` types)
- All components ready for composition in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/07-home-screen/07-01-SUMMARY.md`
</output>
