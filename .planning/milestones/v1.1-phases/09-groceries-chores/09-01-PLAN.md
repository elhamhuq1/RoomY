---
phase: 09-groceries-chores
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/groceries/QuickAddInput.tsx
  - components/groceries/GroceryItemRow.tsx
  - components/groceries/SectionHeader.tsx
  - components/groceries/EmptyState.tsx
  - components/groceries/index.ts
  - app/(app)/(tabs)/groceries.tsx
autonomous: true
requirements: [GRUI-01, GRUI-02, GRUI-03, GRUI-04]

must_haves:
  truths:
    - "Grocery list splits into TO GET and DONE sections with item counts in overline headers"
    - "Grocery items show circle checkboxes (empty circle unchecked, brand-filled with checkmark when checked)"
    - "Each grocery item row shows a member avatar indicating who added the item"
    - "Quick-add input uses card-styled field with branded square add button"
    - "DONE section is collapsed by default, tap header to expand"
    - "Checked items move immediately to the DONE section"
  artifacts:
    - path: "components/groceries/QuickAddInput.tsx"
      provides: "Card-styled input with branded square add button"
      min_lines: 20
    - path: "components/groceries/GroceryItemRow.tsx"
      provides: "Circle checkbox + item name + avatar row with swipe-to-delete"
      min_lines: 30
    - path: "components/groceries/SectionHeader.tsx"
      provides: "Overline header with count and optional chevron for collapsible sections"
      min_lines: 15
    - path: "components/groceries/EmptyState.tsx"
      provides: "Empty grocery list state with cart icon"
      min_lines: 15
    - path: "components/groceries/index.ts"
      provides: "Barrel export for all grocery components"
      min_lines: 4
    - path: "app/(app)/(tabs)/groceries.tsx"
      provides: "Parent screen with data fetching, profile lookup, collapsible DONE section"
      contains: "creatorProfiles"
  key_links:
    - from: "app/(app)/(tabs)/groceries.tsx"
      to: "components/groceries/GroceryItemRow.tsx"
      via: "import and render with item + profile props"
      pattern: "GroceryItemRow"
    - from: "components/groceries/GroceryItemRow.tsx"
      to: "components/ui/Avatar.tsx"
      via: "Avatar component for creator display"
      pattern: "Avatar.*userId"
    - from: "app/(app)/(tabs)/groceries.tsx"
      to: "supabase profiles table"
      via: "batch fetch creator profiles after items load"
      pattern: "profiles.*in.*creatorIds"
---

<objective>
Restyle the grocery screen with design system components: extract presentational components (QuickAddInput, GroceryItemRow, SectionHeader, EmptyState), replace square checkboxes with circle checkboxes, add member avatars showing who added each item, make DONE section collapsible, and apply Card-styled quick-add input.

Purpose: Grocery tab gets consistent visual identity with Phase 6 design system -- circle checkboxes, Avatar component, Card containers, overline typography.
Output: 5 new component files in components/groceries/, rewritten groceries.tsx parent screen.
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-groceries-chores/09-CONTEXT.md
@.planning/phases/09-groceries-chores/09-RESEARCH.md
@.planning/phases/08-expenses-screen/08-01-SUMMARY.md

@components/ui/Avatar.tsx
@components/ui/Card.tsx
@components/ui/index.ts
@components/expenses/index.ts
@lib/theme/colors.ts
@lib/types/database.ts
@app/(app)/(tabs)/groceries.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create grocery presentational components</name>
  <files>
    components/groceries/QuickAddInput.tsx
    components/groceries/GroceryItemRow.tsx
    components/groceries/SectionHeader.tsx
    components/groceries/EmptyState.tsx
    components/groceries/index.ts
  </files>
  <action>
Create `components/groceries/` directory with 4 presentational components and a barrel export, following the Phase 8 `components/expenses/` decomposition pattern. All components are presentational -- no data fetching, typed props only.

**QuickAddInput.tsx** -- Card-styled input with branded square add button:
- Props: `{ value: string; onChangeText: (text: string) => void; onSubmit: () => void }`
- Wrap in `Card` component with `className="mx-4 mt-4 p-0"`
- Inner layout: `flex-row items-center p-2`
- TextInput: `flex-1 px-3 py-2 text-body text-neutral-text`, placeholder "Add an item...", placeholderTextColor from colors.neutral.tertiary, returnKeyType="done", onSubmitEditing calls onSubmit
- Add button: `Pressable` with `h-10 w-10 rounded-xl items-center justify-center`, conditional className -- `bg-brand` when value.trim() is truthy, `bg-neutral-surface` when empty. Disabled when empty. Ionicons "add" size 22, color white when active, colors.neutral.tertiary when disabled.
- Per CONTEXT.md: Enter/Return submits. Do NOT call Keyboard.dismiss() after submit -- keep keyboard open for fast multi-item entry.

**GroceryItemRow.tsx** -- Compact one-line row with circle checkbox, name, and avatar:
- Props: `{ item: GroceryItem; isChecked: boolean; creatorName: string; creatorId: string; onToggle: () => void; onEdit: () => void; onDelete: () => void }`
- Wrap entire row in `ReanimatedSwipeable` for swipe-to-delete (preserve existing pattern from current groceries.tsx -- renderRightActions with red delete button)
- Inner Pressable calls onEdit on press (tap-to-edit via modal stays)
- Circle checkbox: `Pressable` with hitSlop={8}, inner `View` sized `h-6 w-6 rounded-full items-center justify-center`. When unchecked: `border-2 border-neutral-tertiary`. When checked: `bg-brand` with Ionicons "checkmark" size 14 color white.
- Item name: `Text` with `flex-1 text-body` className. When checked: add `text-neutral-tertiary line-through` and `opacity-60` on the whole row.
- Avatar on the right: `<Avatar userId={creatorId} name={creatorName} size="xs" />` from `@/components/ui/Avatar`
- Remove the inline QuantityStepper from the row -- quantity editing stays in the edit modal only per CONTEXT.md. Remove the pencil-outline icon (avatar replaces it as right element). Keep quantity badge for checked items with qty > 1 (`x{quantity}` text between name and avatar).

**SectionHeader.tsx** -- Overline header with count, optional collapsible chevron:
- Props: `{ label: string; count: number; collapsible?: boolean; expanded?: boolean; onToggle?: () => void }`
- Wrapper: `Pressable` (disabled when not collapsible) with `flex-row items-center justify-between px-4 py-2`
- Text: `text-overline text-neutral-secondary uppercase` showing `{label} ({count})`
- When collapsible: show Ionicons chevron-up/chevron-down size 16, color colors.neutral.secondary
- Use LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut) inside onToggle for smooth expand/collapse (imported from react-native)

**EmptyState.tsx** -- Empty grocery list card:
- Props: none (purely visual)
- Centered layout: View with `flex-1 items-center justify-center px-8`
- Icon circle: `h-24 w-24 rounded-full bg-brand-light items-center justify-center` with Ionicons "cart" size 48 color brand
- Heading: `text-section-heading text-neutral-text` "Your grocery list is empty"
- Subtext: `text-body text-neutral-secondary mt-3 text-center` "Add items above to get started"

**index.ts** -- Barrel export all 4 components:
```
export { QuickAddInput } from './QuickAddInput';
export { GroceryItemRow } from './GroceryItemRow';
export { SectionHeader } from './SectionHeader';
export { EmptyState } from './EmptyState';
```
  </action>
  <verify>
    <automated>ls components/groceries/QuickAddInput.tsx components/groceries/GroceryItemRow.tsx components/groceries/SectionHeader.tsx components/groceries/EmptyState.tsx components/groceries/index.ts && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>All 5 files exist and TypeScript compiles without errors</manual>
  </verify>
  <done>4 presentational grocery components and barrel export exist, TypeScript compiles cleanly, all components accept typed props matching GroceryItem type</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite grocery parent screen with new components and profile lookup</name>
  <files>
    app/(app)/(tabs)/groceries.tsx
  </files>
  <action>
Rewrite `app/(app)/(tabs)/groceries.tsx` to use the new presentational components. Preserve ALL existing data fetching, realtime subscriptions, optimistic updates, edit modal, and complete trip logic exactly as-is. Only change the rendering layer.

**Profile batch fetch for creator avatars (GRUI-03):**
- Add state: `const [creatorProfiles, setCreatorProfiles] = useState<Record<string, { id: string; display_name: string }>>({})`
- After `fetchItems` succeeds and sets items, collect unique `created_by` IDs: `const creatorIds = [...new Set(data.map((i: GroceryItem) => i.created_by))]`
- Batch fetch from profiles table: `supabase.from('profiles').select('id, display_name').in('id', creatorIds)`
- Build `Record<string, Profile>` map and set state. Use the established two-query pattern from the expenses/chores screens.
- Also update the realtime INSERT handler: when a new item arrives with an unknown created_by, fetch that single profile and add to the map.

**Collapsible DONE section (CONTEXT.md locked decision):**
- Add state: `const [doneExpanded, setDoneExpanded] = useState(false)`
- DONE section shows `SectionHeader` with `collapsible={true} expanded={doneExpanded} onToggle={() => setDoneExpanded(prev => !prev)}`
- Only render checked item rows when `doneExpanded` is true

**Section rendering:**
- Replace "To Buy" text with `<SectionHeader label="TO GET" count={uncheckedItems.length} />` (not collapsible)
- Replace "Completed" text with `<SectionHeader label="DONE" count={checkedItems.length} collapsible expanded={doneExpanded} onToggle={...} />`
- Wrap item groups in `Card` with `className="mx-4 p-0 overflow-hidden"` (consistent with Phase 8 HistorySection pattern)

**Quick-add input:**
- Replace the old inline input+add-circle-icon with `<QuickAddInput value={newItemName} onChangeText={setNewItemName} onSubmit={addItem} />`
- Remove the old `Keyboard.dismiss()` call from `addItem` function -- keyboard stays open for fast multi-item entry per CONTEXT.md

**Item rows:**
- Replace `renderItemRow()` with `<GroceryItemRow>` component instances
- Pass `creatorName={creatorProfiles[item.created_by]?.display_name ?? '?'}` and `creatorId={item.created_by}`
- Remove inline QuantityStepper from rows (quantity editing stays in modal only)
- Keep the updateQuantity function for the edit modal but remove inline row quantity steppers

**Empty state:**
- Replace inline empty state JSX with `<EmptyState />` component

**Complete Trip button:**
- Restyle with `Button` component from `@/components/ui` or keep the existing Pressable but update colors to use design system classes (bg-brand, text-white). Keep its position as sticky bottom.

**Remove dead code:**
- Remove the `QuantityStepper` component definition (no longer used in rows)
- Remove the `RightSwipeAction` component definition (moved into GroceryItemRow)
- Keep the edit modal and its QuantityStepper (quantity editing stays in modal)

**Preserve exactly:**
- All useCallback functions (addItem, toggleCheck, deleteItem, updateQuantity, saveEdit)
- Realtime subscription channel setup and handlers
- useFocusEffect refetch pattern
- Edit modal with QuantityStepper (still needed for editing)
- Complete Trip bottom button and navigation
- KeyboardAvoidingView wrapper
- ScrollView with keyboardShouldPersistTaps="handled"
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30 && npx expo export --platform ios --no-minify 2>&1 | tail -5</automated>
    <manual>Open grocery tab in Expo Go: verify circle checkboxes, TO GET/DONE section headers with counts, collapsed DONE section, member avatars on rows, card-styled add input with branded square button</manual>
  </verify>
  <done>Grocery screen renders with circle checkboxes, TO GET/DONE overline headers with counts, collapsible DONE section (collapsed by default), member avatars showing item creator, card-styled quick-add input with branded add button, keyboard stays open after adding items</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx expo export --platform ios --no-minify` builds successfully
- All 5 new component files exist in components/groceries/
- groceries.tsx imports from components/groceries barrel export
- No AVATAR_COLORS or getInitials references remain in groceries.tsx
- No square checkbox icons (checkbox/square-outline) remain in groceries.tsx
- Circle checkbox uses brand color when checked
- Avatar component imported from @/components/ui
- QuantityStepper only appears in edit modal, not in item rows
</verification>

<success_criteria>
1. Grocery list shows "TO GET (N)" and "DONE (N)" overline section headers
2. Circle checkboxes render (empty circle unchecked, green filled with checkmark when checked)
3. Each item row shows the creator's avatar on the right via Avatar component
4. Quick-add input is wrapped in Card with branded square add button
5. DONE section is collapsed by default, expands on header tap with smooth animation
6. Keyboard stays open after adding an item for fast multi-item entry
7. All existing functionality preserved: add, check, edit, delete, complete trip, realtime sync
</success_criteria>

<output>
After completion, create `.planning/phases/09-groceries-chores/09-01-SUMMARY.md`
</output>
