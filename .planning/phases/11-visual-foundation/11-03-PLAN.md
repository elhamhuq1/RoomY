---
phase: 11-visual-foundation
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/_layout.tsx
  - app/(app)/_layout.tsx
  - app/(app)/(tabs)/_layout.tsx
  - app.json
autonomous: false
requirements:
  - VIS-06

must_haves:
  truths:
    - "Tab bar background is cream (#F5F0EB) with no visible seam against content"
    - "Active tab icon and label are wintergreen (#2D6A4F), inactive tabs are warm gray"
    - "All screen headers have cream background with dark text"
    - "Status bar shows dark content (dark text/icons) on cream background"
    - "Splash screen background matches cream (#F5F0EB) for seamless app load"
    - "No visible color seam between content area and system chrome on any screen"
  artifacts:
    - path: "app/(app)/(tabs)/_layout.tsx"
      provides: "Cream tab bar with wintergreen active state"
      contains: "#F5F0EB"
    - path: "app/_layout.tsx"
      provides: "StatusBar dark style + cream root background"
      contains: "StatusBar"
    - path: "app.json"
      provides: "Cream splash background"
      contains: "#F5F0EB"
  key_links:
    - from: "app/(app)/(tabs)/_layout.tsx"
      to: "lib/theme/colors.ts"
      via: "colors.neutral.bg for header backgrounds"
      pattern: "colors\\.neutral\\.bg"
    - from: "app/_layout.tsx"
      to: "expo-status-bar"
      via: "StatusBar component import"
      pattern: "import.*StatusBar.*from.*expo-status-bar"
---

<objective>
Update all system chrome (tab bar, headers, status bar, splash screen) to match the cream background with wintergreen accents, then verify the entire phase visually on device.

Purpose: System chrome is the "frame" around the app content. Even if all content uses cream and wintergreen, a mismatched tab bar, header, or status bar creates a visible seam. This plan ensures the entire visual experience is seamless from splash to every screen.

Output: Unified cream system chrome, dark status bar content, wintergreen tab active states, visual verification checkpoint.
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-visual-foundation/11-CONTEXT.md
@.planning/phases/11-visual-foundation/11-RESEARCH.md
@.planning/phases/11-visual-foundation/11-01-SUMMARY.md
@app/_layout.tsx
@app/(app)/_layout.tsx
@app/(app)/(tabs)/_layout.tsx
@app.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update system chrome to cream background with wintergreen accents</name>
  <files>app/_layout.tsx, app/(app)/_layout.tsx, app/(app)/(tabs)/_layout.tsx, app.json</files>
  <action>
**1. Tab bar styling — `app/(app)/(tabs)/_layout.tsx`:**

Update the tab bar configuration:
- `tabBarStyle.backgroundColor`: change from `colors.white` (or `#FFFFFF`) to `'#F5F0EB'` (cream). Alternatively use `colors.neutral.bg` if imported.
- `tabBarStyle.borderTopColor`: change to `'#D6D0C8'` (warm gray) or use `colors.neutral.border`
- `tabBarActiveTintColor`: should already use `colors.brand.DEFAULT` — verify it resolves to wintergreen after Plan 01 token update
- `tabBarInactiveTintColor`: change to `'#9C9590'` (warm gray, complements cream). This replaces any cool slate gray.
- If the header in this layout uses `backgroundColor: colors.white`, change to `colors.neutral.bg` so it auto-tracks the cream token.

**2. Stack headers — `app/(app)/_layout.tsx`:**

The research shows 14 `headerStyle` entries already use `colors.neutral.bg` — these auto-update from Plan 01. Verify this and make no unnecessary changes.

If any header uses `colors.white` directly, change to `colors.neutral.bg`.

**3. Root layout — `app/_layout.tsx`:**

- Add StatusBar import: `import { StatusBar } from 'expo-status-bar';`
- Add `<StatusBar style="dark" />` inside the root GestureHandlerRootView (or equivalent root wrapper), before other children. This ensures dark text/icons on the cream status bar area.
- Find any hardcoded background color like `backgroundColor: "#F8FAFC"` and replace with `colors.neutral.bg` reference (import colors if needed). This is reported at ~line 109.
- The `bg-neutral-bg` NativeWind class (~line 78) auto-updates — no change needed.

**4. Splash screen — `app.json`:**

- Update `expo.splash.backgroundColor` from current value (`#fefdfb` or similar) to `"#F5F0EB"` (cream). This eliminates any flash between splash and first screen.
- If there's an `expo.android.splash.backgroundColor`, update that too.
- Do NOT change any other app.json values (name, slug, icon, etc.).

**5. Scene container backgrounds:**

If any layout file sets a `sceneContainerStyle` or `contentStyle` with `backgroundColor`, ensure it uses cream (#F5F0EB) or `colors.neutral.bg`.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && grep -c 'StatusBar' app/_layout.tsx && grep '#F5F0EB' app.json | head -1 && grep -c "colors.white" "app/(app)/(tabs)/_layout.tsx" | grep '^0$'</automated>
    <manual>StatusBar imported and rendered in root layout, app.json splash uses cream, tab bar uses cream not white</manual>
  </verify>
  <done>
- Tab bar background is cream (#F5F0EB) with warm gray border and wintergreen active tint
- Inactive tab color is warm gray (#9C9590)
- All stack headers use colors.neutral.bg (cream via token)
- StatusBar component renders with style="dark" in root layout
- Root layout background uses colors.neutral.bg (no hardcoded #F8FAFC)
- app.json splash backgroundColor is #F5F0EB
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of entire visual foundation</name>
  <files>None (visual verification only)</files>
  <action>
Human visual verification of the complete visual identity migration. This checkpoint verifies all 3 plans of Phase 11.

**What was built (Plans 01-03):**
- Wintergreen (#2D6A4F) brand color on all buttons, badges, tab bar, FAB, toggles
- Cream (#F5F0EB) background on all screens and system chrome
- Warm gray (#D6D0C8) outline cards with no shadows
- Earth-tone avatar gradients (wintergreen, terracotta, amber, slate, plum, ocean, clay, sage)
- Seamless cream system chrome (tab bar, headers, status bar, splash)

**How to verify:**
1. Start the app with `npx expo start` and open in Expo Go
2. **Splash screen**: Verify cream background, no color flash on load
3. **Auth/Welcome screen**: Verify wintergreen buttons, no emerald green
4. **Home tab**: Check cream background, wintergreen accents, outline cards (no shadows), gradient balance card still dark
5. **Expenses tab**: Check outline containers, no shadow+outline conflicts, AVATAR colors are earth tones
6. **Groceries tab**: Check cream background, brand checkboxes are wintergreen
7. **Chores tab**: Check wintergreen indicators, earth-tone avatars, outline cards
8. **Settings**: Check outline containers, no shadows, cream background
9. **Tab bar**: Verify cream background (no seam with content), wintergreen active tab, warm gray inactive tabs
10. **Status bar**: Dark text/icons on cream
11. **Scroll any screen**: Verify no color seam between content and tab bar / header
12. **Look for any stale emerald green (#10B981) anywhere in the app**

**Resume signal:** Type "approved" if the visual identity looks cohesive and correct, or describe any issues you see (stale colors, seams, shadow/outline conflicts, etc.)
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && grep -rn '#10B981\|#059669\|#D1FAE5' --include='*.tsx' --include='*.ts' app/ components/ lib/ | wc -l</automated>
    <manual>User visually confirms no stale emerald, no seams, cohesive wintergreen+cream identity</manual>
  </verify>
  <done>
- User approves the visual identity as cohesive and correct
- No stale emerald green visible anywhere in the app
- No visible color seams between content and system chrome
- Cards are flat with outlines only (no shadows)
- All avatars show earth-tone gradients
  </done>
</task>

</tasks>

<verification>
1. Tab bar background matches cream (#F5F0EB)
2. Active tab tint is wintergreen (#2D6A4F)
3. Inactive tab tint is warm gray (#9C9590)
4. StatusBar renders with dark style in root layout
5. app.json splash background is #F5F0EB
6. No `colors.white` references in tab bar configuration
7. Visual verification confirms no seams, no stale emerald, cohesive cream+wintergreen identity
</verification>

<success_criteria>
- System chrome is seamless cream with wintergreen accents
- No visible color seam on any screen between content and chrome
- Status bar shows dark content on cream
- Splash transitions smoothly to first screen (no flash)
- Human verification confirms the entire visual foundation is cohesive
</success_criteria>

<output>
After completion, create `.planning/phases/11-visual-foundation/11-03-SUMMARY.md`
</output>
