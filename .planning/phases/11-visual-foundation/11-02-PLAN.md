---
phase: 11-visual-foundation
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/(app)/expenses/settle.tsx
  - app/(app)/expenses/add.tsx
  - app/(app)/expenses/[id].tsx
  - app/(app)/chores/swap-request.tsx
  - app/(app)/chores/dashboard.tsx
  - app/(app)/chores/add.tsx
  - app/(app)/groceries/complete-trip.tsx
  - app/(app)/settings/members.tsx
  - app/(auth)/welcome.tsx
  - components/ui/Toggle.tsx
  - components/chores/ChoreRow.tsx
  - app/(app)/settings/index.tsx
  - app/(app)/settings/notifications.tsx
  - app/(app)/settings/modules.tsx
  - app/(app)/groceries/trip-history.tsx
  - components/home/CalendarSection.tsx
  - app/(app)/groceries.tsx
  - app/(app)/chores/add.tsx
autonomous: true
requirements:
  - VIS-03
  - VIS-04
  - CARD-02
  - CARD-03

must_haves:
  truths:
    - "No hardcoded emerald hex values (#10B981, #059669, #D1FAE5) remain in any .ts/.tsx file under app/, components/, or lib/"
    - "All 8 files that had local AVATAR_COLORS arrays now import from @/lib/theme/colors"
    - "No container in the app displays both shadow and outline simultaneously"
    - "Gradient balance card and dark invite code card retain their distinctive elevated styling unchanged"
  artifacts:
    - path: "app/(app)/expenses/settle.tsx"
      provides: "Shared AVATAR_COLORS import + outline container"
      contains: "import.*AVATAR_COLORS.*from.*colors"
    - path: "components/ui/Toggle.tsx"
      provides: "Wintergreen active state color"
      contains: "colors.brand.DEFAULT"
    - path: "components/chores/ChoreRow.tsx"
      provides: "Wintergreen indicator and icon colors"
      contains: "colors.brand.DEFAULT"
    - path: "app/(auth)/welcome.tsx"
      provides: "Wintergreen brand colors in onboarding"
      contains: "#2D6A4F"
  key_links:
    - from: "app/(app)/expenses/settle.tsx"
      to: "lib/theme/colors.ts"
      via: "AVATAR_COLORS import"
      pattern: "import.*AVATAR_COLORS.*from.*@/lib/theme/colors"
    - from: "components/ui/Toggle.tsx"
      to: "lib/theme/colors.ts"
      via: "colors.brand.DEFAULT reference"
      pattern: "colors\\.brand\\.DEFAULT"
---

<objective>
Replace all hardcoded emerald hex values and local AVATAR_COLORS duplicates across the entire app, consolidating to shared imports. Then audit and restyle all non-Card containers to use outline-only styling, ensuring no container has both shadow and outline.

Purpose: Plan 01 updated the token definitions and shared components. This plan propagates those changes to the ~17 hardcoded hex occurrences and ~11 non-Card containers that bypass the token system, ensuring visual consistency throughout the app.

Output: Zero hardcoded emerald hex values, all AVATAR_COLORS arrays consolidated, all containers using consistent outline-only styling.
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-visual-foundation/11-CONTEXT.md
@.planning/phases/11-visual-foundation/11-RESEARCH.md
@.planning/phases/11-visual-foundation/11-01-SUMMARY.md
@lib/theme/colors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all hardcoded emerald hex values and consolidate AVATAR_COLORS imports</name>
  <files>
    app/(app)/expenses/settle.tsx
    app/(app)/expenses/add.tsx
    app/(app)/expenses/[id].tsx
    app/(app)/chores/swap-request.tsx
    app/(app)/chores/dashboard.tsx
    app/(app)/chores/add.tsx
    app/(app)/groceries/complete-trip.tsx
    app/(app)/settings/members.tsx
    app/(auth)/welcome.tsx
    components/ui/Toggle.tsx
    components/chores/ChoreRow.tsx
  </files>
  <action>
**AVATAR_COLORS consolidation (8 files):**

In each of these 8 files, remove the local `const AVATAR_COLORS = [...]` array and replace with an import:

Files: `expenses/settle.tsx`, `expenses/add.tsx`, `expenses/[id].tsx`, `chores/swap-request.tsx`, `chores/dashboard.tsx`, `chores/add.tsx`, `groceries/complete-trip.tsx`, `settings/members.tsx`

For each file:
1. Add `import { AVATAR_COLORS } from '@/lib/theme/colors';` (if the file already imports `colors` from that path, add `AVATAR_COLORS` to the existing import)
2. Delete the local `const AVATAR_COLORS = [...]` declaration entirely
3. Verify the rest of the file references `AVATAR_COLORS` by name (no changes needed — the variable name is the same)

**Hardcoded hex in welcome.tsx (3 occurrences):**
- Line ~186: active dot color `#10B981` -> `#2D6A4F` (wintergreen primary)
- Line ~198: Get Started button background `#10B981` -> `#2D6A4F`
- Line ~217: "Log in" text color `#059669` -> `#1B4332` (deep forest)

If welcome.tsx doesn't already import `colors`, add the import and use `colors.brand.DEFAULT` and `colors.brand.dark` instead of hardcoded hex where possible. If the values are used in inline styles, prefer the colors import. If they're used in NativeWind class strings, use `bg-brand` / `text-brand-dark` classes instead.

**Hardcoded hex in Toggle.tsx (1 occurrence):**
- Line ~29: `interpolateColor` active state uses `#10B981`
- Replace with `colors.brand.DEFAULT` reference. If Toggle.tsx doesn't already import colors, add: `import { colors } from '@/lib/theme/colors';`

**Hardcoded hex in ChoreRow.tsx (3 occurrences):**
- Lines ~182, ~184: `ActivityIndicator` color and checkmark icon color use `#10B981` -> replace with `colors.brand.DEFAULT`
- Line ~232: icon color uses `#10B981` -> replace with `colors.brand.DEFAULT`
- If there's an `active:bg-emerald-200` class on a Pressable: replace with `active:bg-brand-light` (resolves to #D8E8DC sage-green wash)
- If ChoreRow.tsx doesn't already import colors, add the import

**Verification step after all changes:**
Run `grep -rn '#10B981\|#059669\|#D1FAE5' --include='*.tsx' --include='*.ts' app/ components/ lib/` and confirm zero results. The ONLY place these old hex values should appear is in git history.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && grep -rn '#10B981\|#059669\|#D1FAE5' --include='*.tsx' --include='*.ts' app/ components/ lib/ | wc -l</automated>
    <manual>Should output 0 — no hardcoded emerald values remain anywhere</manual>
  </verify>
  <done>
- All 8 files with local AVATAR_COLORS now import from @/lib/theme/colors
- Zero local AVATAR_COLORS declarations remain
- welcome.tsx uses wintergreen colors (#2D6A4F / #1B4332) or colors.brand references
- Toggle.tsx uses colors.brand.DEFAULT for active state
- ChoreRow.tsx uses colors.brand.DEFAULT for all indicator/icon colors
- `grep -r '#10B981\|#059669\|#D1FAE5' --include='*.tsx' --include='*.ts' app/ components/ lib/` returns zero results
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle non-Card containers to outline-only and verify CARD-02/CARD-03</name>
  <files>
    app/(app)/expenses/settle.tsx
    app/(app)/expenses/[id].tsx
    app/(app)/settings/index.tsx
    app/(app)/settings/notifications.tsx
    app/(app)/settings/members.tsx
    app/(app)/settings/modules.tsx
    app/(app)/chores/dashboard.tsx
    app/(app)/groceries/trip-history.tsx
    components/home/CalendarSection.tsx
    app/(app)/groceries.tsx
    app/(app)/chores/add.tsx
  </files>
  <action>
Audit and restyle all non-Card containers that currently use `bg-white shadow-sm` or `bg-white shadow` to use the outline-only pattern: `bg-transparent rounded-card border border-neutral-border`.

**Pattern for each container:**
- Remove `bg-white` -> add `bg-transparent`
- Remove `shadow-sm` or `shadow`
- Remove any Android `elevation` inline style
- Add `border border-neutral-border` if not already present
- Change `rounded-2xl` to `rounded-card` for consistency (12px per token system)

**Specific files and locations (from research):**

| File | Current | Target |
|------|---------|--------|
| `expenses/settle.tsx` ~line 208 | `rounded-2xl bg-white p-6 shadow-sm` | `rounded-card bg-transparent border border-neutral-border p-6` |
| `expenses/[id].tsx` ~lines 392, 650, 678 | `rounded-2xl bg-white p-6 shadow-sm` | `rounded-card bg-transparent border border-neutral-border p-6` |
| `settings/index.tsx` ~lines 81, 96 | `rounded-2xl bg-white p-5 shadow-sm` | `rounded-card bg-transparent border border-neutral-border p-5` |
| `settings/notifications.tsx` ~line 89 | `rounded-2xl bg-white shadow-sm` | `rounded-card bg-transparent border border-neutral-border` |
| `settings/members.tsx` ~lines 204, 269 | `rounded-2xl bg-white shadow-sm` | `rounded-card bg-transparent border border-neutral-border` |
| `settings/modules.tsx` ~line 162 | `bg-white p-5 shadow-sm` | `bg-transparent border border-neutral-border p-5` |
| `chores/dashboard.tsx` ~lines 369, 398 | `rounded-xl bg-white px-6 py-8` | `rounded-card bg-transparent border border-neutral-border px-6 py-8` |
| `groceries/trip-history.tsx` ~line 142 | `rounded-xl bg-white p-4 shadow-sm` | `rounded-card bg-transparent border border-neutral-border p-4` |
| `CalendarSection.tsx` ~line 140 | `bg-white shadow-sm` or similar | `bg-transparent border border-neutral-border` |

**Bottom bars (seamless cream, not outline):**
| File | Current | Target |
|------|---------|--------|
| `groceries.tsx` bottom bar | `border-t border-neutral-border bg-white` | `border-t border-neutral-border bg-[#F5F0EB]`  (cream for seamlessness) |
| `chores/add.tsx` bottom bar | `border-t border-gray-100 bg-white` | `border-t border-neutral-border bg-[#F5F0EB]` |

**IMPORTANT EXCEPTIONS — DO NOT MODIFY:**
1. **Gradient balance card** (`components/home/BalanceSummaryCard.tsx`): Uses LinearGradient with `['#1E293B', '#0F172A']` — this is intentionally dark/elevated. Do NOT touch it (CARD-02).
2. **Dark invite code card** (`app/(app)/onboarding/create-household.tsx`): Uses LinearGradient — Do NOT touch it (CARD-02).
3. **Bottom sheets / modals** (e.g., `chores.tsx` with `rounded-t-3xl bg-white`): Keep `bg-white` — overlays need visual separation from cream surface.
4. **TextInput containers**: Any `bg-white` on input fields should stay — inputs need contrast against cream for interactivity indication.

**Post-change audit:**
Run `grep -rn 'shadow-sm\|shadow ' --include='*.tsx' app/ components/` and review each remaining match. Remaining shadows should ONLY be on:
- Bottom sheets / modals
- The gradient balance card (LinearGradient, not shadow class)
- The boxShadow token definitions in tailwind.config.js (these are unused after Card.tsx update but harmless to keep)

No container should have both `shadow` and `border-neutral-border` simultaneously (CARD-03).
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && grep -rn 'bg-white.*shadow\|shadow.*bg-white' --include='*.tsx' app/ components/ | grep -v 'rounded-t\|modal\|sheet\|Modal\|Sheet\|BottomSheet' | wc -l</automated>
    <manual>Should be 0 or near-0 — only modals/sheets should have shadow+white. No container has both shadow and outline.</manual>
  </verify>
  <done>
- All non-Card containers use bg-transparent + border border-neutral-border (no shadow)
- Bottom bars use cream background (#F5F0EB) for seamless integration
- Gradient balance card and dark invite code card are untouched (CARD-02)
- Bottom sheets and modals retain bg-white (appropriate separation)
- No container displays both shadow and outline simultaneously (CARD-03)
- TextInput containers retain bg-white for interactivity contrast
  </done>
</task>

</tasks>

<verification>
1. `grep -rn '#10B981\|#059669\|#D1FAE5' --include='*.tsx' --include='*.ts' app/ components/ lib/` returns zero results (VIS-03)
2. `grep -rn 'const AVATAR_COLORS' --include='*.tsx' --include='*.ts' app/ components/` returns zero results — no local declarations (VIS-04)
3. `grep -rn 'AVATAR_COLORS' --include='*.tsx' --include='*.ts' app/ components/` shows only import statements (VIS-04)
4. No file has both `shadow` class and `border-neutral-border` on the same element (CARD-03)
5. BalanceSummaryCard.tsx and create-household.tsx LinearGradient styling is unchanged (CARD-02)
</verification>

<success_criteria>
- Zero hardcoded emerald hex values in app/, components/, or lib/
- AVATAR_COLORS consolidated: single export in colors.ts, 8 import consumers
- All non-Card containers restyled to outline-only
- Special cards (gradient balance, dark invite) preserved
- No shadow+outline violations anywhere in the app
</success_criteria>

<output>
After completion, create `.planning/phases/11-visual-foundation/11-02-SUMMARY.md`
</output>
