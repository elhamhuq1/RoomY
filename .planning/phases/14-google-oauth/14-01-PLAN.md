---
phase: 14-google-oauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auth-utils.ts
  - app/(auth)/sign-in.tsx
  - app/(auth)/sign-up.tsx
  - app.json
  - assets/google-g-logo.png
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
user_setup:
  - service: supabase
    why: "OAuth redirect URL allowlist"
    dashboard_config:
      - task: "Add redirect URLs to allowlist"
        location: "Supabase Dashboard > Authentication > URL Configuration > Redirect URLs"
        details: "Add `com.roomy://**` and `exp://**` patterns"
      - task: "Verify Google provider is enabled"
        location: "Supabase Dashboard > Authentication > Providers > Google"

must_haves:
  truths:
    - "User can tap Google button and complete sign-in via browser OAuth"
    - "Google OAuth works in Expo Go without native module crash"
    - "Apple sign-in button and code are completely removed"
    - "Google-authenticated users get their Google profile picture automatically"
    - "Cancelling mid-flow returns silently to auth screen"
    - "Email/password auth continues to work unchanged"
  artifacts:
    - path: "lib/auth-utils.ts"
      provides: "Browser-based Google OAuth using expo-web-browser"
      contains: "signInWithOAuth"
    - path: "app/(auth)/sign-in.tsx"
      provides: "Sign-in screen with Google branded button, no Apple button"
      contains: "google-g-logo"
    - path: "app/(auth)/sign-up.tsx"
      provides: "Sign-up screen with Google branded button, no Apple button"
      contains: "google-g-logo"
    - path: "app.json"
      provides: "Clean plugins array without google-signin or apple-authentication"
    - path: "assets/google-g-logo.png"
      provides: "Google G logo for branded button"
  key_links:
    - from: "app/(auth)/sign-in.tsx"
      to: "lib/auth-utils.ts"
      via: "import { signInWithGoogle }"
      pattern: "signInWithGoogle"
    - from: "lib/auth-utils.ts"
      to: "expo-web-browser"
      via: "WebBrowser.openAuthSessionAsync"
      pattern: "openAuthSessionAsync"
    - from: "lib/auth-utils.ts"
      to: "@supabase/supabase-js"
      via: "supabase.auth.signInWithOAuth"
      pattern: "signInWithOAuth"
---

<objective>
Replace native Google Sign-In with browser-based OAuth flow using expo-web-browser, remove all Apple sign-in code, and update auth screen UI with Google's branded button.

Purpose: Enable Google sign-in that works in Expo Go without native modules, completing the v1.2 auth story.
Output: Working Google OAuth flow on both sign-in and sign-up screens.
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-google-oauth/14-CONTEXT.md
@.planning/phases/14-google-oauth/14-RESEARCH.md
@lib/auth-utils.ts
@app/(auth)/sign-in.tsx
@app/(auth)/sign-up.tsx
@app.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace auth-utils with browser OAuth and clean up packages</name>
  <files>lib/auth-utils.ts, app.json, package.json, assets/google-g-logo.png</files>
  <action>
1. Install new packages and uninstall old ones:
   ```
   npx expo install expo-web-browser expo-auth-session
   npm uninstall @react-native-google-signin/google-signin expo-apple-authentication
   ```

2. Remove plugins from app.json `plugins` array:
   - Remove the `@react-native-google-signin/google-signin` plugin entry (the array with iosUrlScheme config)
   - Remove `expo-apple-authentication`
   - Keep: `expo-router`, `expo-sqlite`, `expo-font`

3. Add Google "G" logo asset:
   - Download the official Google "G" logo PNG to `assets/google-g-logo.png`
   - Use a clean 48x48 or 64x64 PNG of the multicolor Google "G" on transparent background
   - If download fails, create a simple placeholder and note it for the checkpoint

4. Completely rewrite `lib/auth-utils.ts`:
   - Remove ALL imports of `@react-native-google-signin/google-signin` and `expo-apple-authentication`
   - Remove the `signInWithApple()` function entirely
   - Remove `Platform` import (no longer needed)
   - Add imports: `expo-web-browser`, `expo-auth-session/build/QueryParams`, `makeRedirectUri` from `expo-auth-session`
   - Call `WebBrowser.maybeCompleteAuthSession()` at module level
   - Generate redirect URI with `makeRedirectUri()` at module level
   - Add `withTimeout()` helper using `Promise.race()` with 10-second timeout
   - Rewrite `signInWithGoogle()` to use the browser-based flow:
     a. Call `supabase.auth.signInWithOAuth({ provider: "google", options: { redirectTo, skipBrowserRedirect: true } })`
     b. Open URL with `WebBrowser.openAuthSessionAsync(data.url, redirectTo, { showInRecents: true })`
     c. If `result.type !== "success"`, return `{ data: null, error: null }` (silent cancel per CONTEXT.md)
     d. Extract tokens with `QueryParams.getQueryParams(result.url)`
     e. Call `supabase.auth.setSession()` wrapped in `withTimeout()`
     f. Handle account conflict: if error message indicates email exists with different provider, replace with "An account with this email already exists. Please sign in with your password."
   - Keep `requestPasswordReset()` unchanged
   - Follow the exact code pattern from 14-RESEARCH.md "Complete auth-utils.ts Replacement Pattern"
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit lib/auth-utils.ts 2>&1 | head -20 && grep -c "signInWithApple\|expo-apple-authentication\|google-signin" lib/auth-utils.ts app.json; echo "Expected: 0 matches"</automated>
    <manual>Check that app.json plugins array only contains expo-router, expo-sqlite, expo-font</manual>
  </verify>
  <done>
    - lib/auth-utils.ts exports signInWithGoogle (browser-based) and requestPasswordReset only
    - No references to native google-signin or apple-authentication in lib/auth-utils.ts or app.json
    - expo-web-browser and expo-auth-session in package.json dependencies
    - Google G logo PNG exists at assets/google-g-logo.png
  </done>
</task>

<task type="auto">
  <name>Task 2: Update auth screens with Google branded button and remove Apple</name>
  <files>app/(auth)/sign-in.tsx, app/(auth)/sign-up.tsx</files>
  <action>
Apply identical changes to BOTH sign-in.tsx and sign-up.tsx:

1. Remove Apple sign-in code:
   - Remove `signInWithApple` from the import of `@/lib/auth-utils`
   - Remove the `handleAppleSignIn()` function
   - Remove `"apple"` from the `socialLoading` state type (change to `"google" | null`)
   - Remove the entire Apple button Pressable block (the `Platform.OS === "ios"` conditional with the black Apple button)
   - Remove the `<View className="mb-6" />` else branch that was paired with the Apple button

2. Update Google button to branded style per CONTEXT.md:
   - Replace `Ionicons` `logo-google` icon with `<Image source={require("@/assets/google-g-logo.png")} style={{ width: 20, height: 20, marginRight: 8 }} />`
   - Change button text from "Continue with Google" to "Sign in with Google"
   - Keep the existing white button style with gray border (already matches Google brand guidelines)
   - Change the `mb-3` on the Google button to `mb-6` (since Apple button below it is gone)

3. Clean up unused imports:
   - If `Ionicons` is no longer used anywhere in the file, remove the import
   - If `Platform` is no longer used (check -- it IS used for KeyboardAvoidingView behavior and BlurView backgroundColor), keep it
   - Ensure `Image` is still imported from react-native (already is in both files)

4. Ensure the "or" divider stays as-is -- it already uses the minimal "or" style the user wants.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit app/\(auth\)/sign-in.tsx app/\(auth\)/sign-up.tsx 2>&1 | head -20 && grep -c "signInWithApple\|handleAppleSignIn\|logo-apple\|expo-apple" app/\(auth\)/sign-in.tsx app/\(auth\)/sign-up.tsx; echo "Expected: all 0"</automated>
    <manual>Visual check: both auth screens show Google branded button with G logo below the form, no Apple button visible</manual>
  </verify>
  <done>
    - Both sign-in and sign-up screens show Google branded button with G logo image
    - Button text reads "Sign in with Google"
    - No Apple sign-in button or dead code on either screen
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Configure Supabase redirect URLs and verify Google OAuth flow</name>
  <what-built>
    Browser-based Google OAuth flow replacing native Google Sign-In. Apple sign-in removed entirely. Google branded button on both auth screens.
  </what-built>
  <how-to-verify>
    1. **Supabase Dashboard setup (required before testing):**
       - Go to Supabase Dashboard > Authentication > URL Configuration > Redirect URLs
       - Add: `com.roomy://**`
       - Add: `exp://**`
       - Verify Google provider is enabled under Authentication > Providers > Google

    2. **Start the app:** `npx expo start` and open in Expo Go

    3. **Visual check on sign-in screen:**
       - Google button appears below email/password form with Google "G" logo
       - No Apple sign-in button visible
       - "or" divider between form and Google button

    4. **Visual check on sign-up screen:**
       - Same Google button layout as sign-in
       - No Apple sign-in button visible

    5. **Test Google OAuth flow:**
       - Tap "Sign in with Google" on sign-in screen
       - Browser should open to Google consent screen
       - Complete Google sign-in
       - App should redirect back and establish session
       - New user should land in onboarding, existing user in home

    6. **Test cancel flow:**
       - Tap Google button, then close browser without completing
       - Should return silently to auth screen with no error message

    7. **Test email/password still works:**
       - Sign in with existing email/password credentials
       - Should work exactly as before
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -r "signInWithApple\|expo-apple-authentication\|@react-native-google-signin" --include="*.ts" --include="*.tsx" --include="*.json" app/ lib/ | grep -v node_modules` returns zero results
- `npx tsc --noEmit` passes without auth-related errors
- Google OAuth flow completes end-to-end in Expo Go
</verification>

<success_criteria>
- User can sign in with Google via browser-based OAuth in Expo Go
- Apple sign-in completely removed (code, imports, plugins, packages)
- Google branded button with G logo on both auth screens
- Cancel flow returns silently
- Email/password auth unaffected
- Supabase redirect URLs configured
</success_criteria>

<output>
After completion, create `.planning/phases/14-google-oauth/14-01-SUMMARY.md`
</output>
