---
phase: 06-design-system-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.js
  - lib/theme/colors.ts
  - app/_layout.tsx
  - app/(app)/_layout.tsx
  - app/(app)/(tabs)/_layout.tsx
  - app/(app)/(tabs)/index.tsx
  - app/(app)/(tabs)/expenses.tsx
  - app/(app)/(tabs)/groceries.tsx
  - app/(app)/(tabs)/chores.tsx
  - app/(auth)/welcome.tsx
  - app/(auth)/sign-up.tsx
  - app/(auth)/sign-in.tsx
  - app/(auth)/forgot-password.tsx
  - app/(onboarding)/profile.tsx
  - app/(onboarding)/setup-choice.tsx
  - app/(onboarding)/create-household.tsx
  - app/(onboarding)/join-household.tsx
  - app/(onboarding)/member-welcome.tsx
  - app/(onboarding)/module-quiz.tsx
  - app/(app)/expenses/add.tsx
  - app/(app)/expenses/detail.tsx
  - app/(app)/expenses/settle.tsx
  - app/(app)/groceries/complete-trip.tsx
  - app/(app)/groceries/trip-history.tsx
  - app/(app)/chores/create.tsx
  - app/(app)/chores/dashboard.tsx
  - app/(app)/chores/swap-request.tsx
  - app/(app)/settings/index.tsx
  - app/(app)/settings/members.tsx
  - app/(app)/settings/modules.tsx
autonomous: true
requirements:
  - DSYS-01
  - DSYS-02
  - DSYS-03

must_haves:
  truths:
    - "No orange hex values (#f9a825, #fefdfb, #faf3e8, #fde4b9, #66bb6a) remain in any source file"
    - "All primary-*, surface-*, and accent-* Tailwind class references replaced with new token names"
    - "8 typography presets produce visually distinct text styles when applied via className"
    - "Two shadow tiers (shadow, shadow-md) are available as Tailwind classes"
  artifacts:
    - path: "tailwind.config.js"
      provides: "Complete design token system (colors, typography, shadows)"
      contains: "brand.*#10B981"
    - path: "lib/theme/colors.ts"
      provides: "Color constants for inline styles and non-Tailwind contexts"
      exports: ["colors"]
  key_links:
    - from: "tailwind.config.js"
      to: "all app/**/*.tsx files"
      via: "NativeWind className resolution"
      pattern: "bg-brand|text-brand|text-neutral|bg-neutral"
    - from: "lib/theme/colors.ts"
      to: "app/(app)/(tabs)/_layout.tsx"
      via: "import for tabBarActiveTintColor and other inline style colors"
      pattern: "import.*colors.*from.*theme/colors"
---

<objective>
Define the complete design token system (colors, typography, elevation) in tailwind.config.js and colors.ts, then migrate every file in the codebase from the old orange/warm palette to the new emerald green brand palette.

Purpose: Every subsequent plan and phase depends on tokens being defined and the old palette removed. This is the foundation of the v1.1 visual redesign.
Output: Updated tailwind.config.js with all tokens, new lib/theme/colors.ts, and all ~28 source files migrated to new palette.
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-design-system-components/06-CONTEXT.md
@.planning/phases/06-design-system-components/06-RESEARCH.md
@tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define design tokens in tailwind.config.js and colors.ts</name>
  <files>
    tailwind.config.js
    lib/theme/colors.ts
  </files>
  <action>
**tailwind.config.js** — Rewrite the `theme.extend` section to replace the old orange palette with the new token system:

Colors (replace existing `primary`, `surface`, `accent` entirely):
```
brand: { DEFAULT: '#10B981', dark: '#059669', light: '#D1FAE5' }
semantic: { success: '#22C55E', warning: '#F59E0B', error: '#EF4444', info: '#3B82F6' }
neutral: { text: '#0F172A', secondary: '#64748B', tertiary: '#94A3B8', border: '#E2E8F0', surface: '#F1F5F9', bg: '#F8FAFC' }
```
Keep brand green SEPARATE from semantic success green per user decision.

fontSize (add all 8 presets using tuple syntax):
```
'page-title': ['28px', { lineHeight: '34px', fontWeight: '700' }]
'key-number': ['32px', { lineHeight: '38px', fontWeight: '700' }]
'section-heading': ['18px', { lineHeight: '24px', fontWeight: '600' }]
'card-title': ['16px', { lineHeight: '22px', fontWeight: '600' }]
'body': ['15px', { lineHeight: '22px', fontWeight: '400' }]
'metadata': ['13px', { lineHeight: '18px', fontWeight: '400' }]
'overline': ['11px', { lineHeight: '14px', fontWeight: '600', letterSpacing: '0.1em' }]
'badge': ['11px', { lineHeight: '14px', fontWeight: '500' }]
```

boxShadow (two-tier elevation):
```
DEFAULT: '0px 1px 3px rgba(0, 0, 0, 0.08), 0px 1px 2px rgba(0, 0, 0, 0.04)'
md: '0px 4px 8px rgba(0, 0, 0, 0.08), 0px 2px 4px rgba(0, 0, 0, 0.04)'
```

borderRadius: Keep existing `xl: '16px'` and `2xl: '24px'`, add `card: '12px'` and `card-lg: '16px'`.

Keep existing `content`, `presets`, `plugins` sections unchanged.

**lib/theme/colors.ts** — Create this new file. Export a `colors` constant with the same hex values as tailwind.config.js for use in inline styles (tabBarActiveTintColor, ActivityIndicator color, Reanimated interpolateColor, expo-linear-gradient colors, etc.):
```typescript
export const colors = {
  brand: { DEFAULT: '#10B981', dark: '#059669', light: '#D1FAE5' },
  semantic: { success: '#22C55E', warning: '#F59E0B', error: '#EF4444', info: '#3B82F6' },
  neutral: { text: '#0F172A', secondary: '#64748B', tertiary: '#94A3B8', border: '#E2E8F0', surface: '#F1F5F9', bg: '#F8FAFC' },
  white: '#FFFFFF',
} as const;
```

Create the `lib/theme/` directory if it doesn't exist.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && node -e "const c = require('./tailwind.config.js'); const t = c.theme.extend; console.log('brand:', !!t.colors.brand, 'fontSize:', Object.keys(t.fontSize).length === 8, 'boxShadow:', !!t.boxShadow)" && node -e "const {colors} = require('./lib/theme/colors.ts'.replace('.ts','.js')); console.log('ERROR: cannot require TS directly')" 2>/dev/null; grep -c "10B981" tailwind.config.js && grep -c "f9a825" tailwind.config.js</automated>
    <manual>Verify tailwind.config.js has brand colors, 8 fontSize presets, boxShadow tiers, and NO orange hex values. Verify lib/theme/colors.ts exists with matching values.</manual>
  </verify>
  <done>tailwind.config.js contains brand/semantic/neutral color tokens, 8 typography presets, 2-tier boxShadow, and updated borderRadius. lib/theme/colors.ts exports matching color constants. Zero orange hex values in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all source files from orange palette to new tokens</name>
  <files>
    app/_layout.tsx
    app/(app)/_layout.tsx
    app/(app)/(tabs)/_layout.tsx
    app/(app)/(tabs)/index.tsx
    app/(app)/(tabs)/expenses.tsx
    app/(app)/(tabs)/groceries.tsx
    app/(app)/(tabs)/chores.tsx
    app/(auth)/welcome.tsx
    app/(auth)/sign-up.tsx
    app/(auth)/sign-in.tsx
    app/(auth)/forgot-password.tsx
    app/(onboarding)/profile.tsx
    app/(onboarding)/setup-choice.tsx
    app/(onboarding)/create-household.tsx
    app/(onboarding)/join-household.tsx
    app/(onboarding)/member-welcome.tsx
    app/(onboarding)/module-quiz.tsx
    app/(app)/expenses/add.tsx
    app/(app)/expenses/detail.tsx
    app/(app)/expenses/settle.tsx
    app/(app)/groceries/complete-trip.tsx
    app/(app)/groceries/trip-history.tsx
    app/(app)/chores/create.tsx
    app/(app)/chores/dashboard.tsx
    app/(app)/chores/swap-request.tsx
    app/(app)/settings/index.tsx
    app/(app)/settings/members.tsx
    app/(app)/settings/modules.tsx
  </files>
  <action>
Perform a comprehensive color migration across ALL source files. This is a mechanical but thorough task.

**Step 1: Grep for all old values.** Find every occurrence of:
- Inline hex: `#f9a825`, `#fefdfb`, `#faf3e8`, `#fde4b9`, `#fef3e2`, `#66bb6a`, `#df5f0a` and all `primary-*` shades
- Tailwind classes: `primary-*` (e.g., `bg-primary-500`, `text-primary-600`, `border-primary-*`), `surface-*` (e.g., `bg-surface-50`, `bg-surface-200`), `accent-*` (e.g., `text-accent-500`)

**Step 2: Replace inline hex values** using `import { colors } from '@/lib/theme/colors'`:
- `#f9a825` (old primary) → `colors.brand.DEFAULT` (for inline styles) or `bg-brand` / `text-brand` (for className)
- `#fefdfb` (old surface-50) → `colors.white` or `colors.neutral.bg` depending on context
- `#faf3e8` (old surface-200) → `colors.neutral.border` (was used for borders/dividers)
- `#fde4b9` (old primary-100) → `colors.brand.light` (was used for light backgrounds)
- `#66bb6a` (old accent-500) → `colors.semantic.success` (was used for success states)
- `#9ca3af` (gray icon color) → `colors.neutral.tertiary`
- `#374151` (header text) → `colors.neutral.text`

**Step 3: Replace Tailwind class references:**
- `bg-primary-500` → `bg-brand`
- `bg-primary-50`, `bg-primary-100` → `bg-brand-light`
- `text-primary-500`, `text-primary-600`, `text-primary-700` → `text-brand` or `text-brand-dark`
- `border-primary-*` → `border-brand` or `border-brand-dark`
- `bg-surface-50` → `bg-white` or `bg-neutral-bg`
- `bg-surface-200` → `bg-neutral-surface`
- `text-accent-500` → `text-semantic-success`

**Step 4: Replace duplicated AVATAR_COLORS arrays.** Files containing AVATAR_COLORS (index.tsx, expenses.tsx, groceries.tsx, chores.tsx, add.tsx, etc.) — remove the local array entirely. These will be centralized in the Avatar component (Plan 02). For now, replace the hardcoded orange-tinted arrays with a temporary import-ready array using the new palette colors. Use:
```typescript
const AVATAR_COLORS = ['#10B981', '#3B82F6', '#8B5CF6', '#EC4899', '#F59E0B', '#EF4444', '#06B6D4', '#84CC16'];
```
(These will be replaced by the Avatar component in Plan 02.)

**Step 5: Update all headerStyle/headerTintColor** in layout files to use `colors.neutral.bg` for background and `colors.neutral.text` for tint.

**Step 6: Verify no old values remain** by running grep for all old hex values.

IMPORTANT: Read each file before editing. Preserve all logic, only change color values and class names. Do NOT change component structure, navigation, or business logic.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && grep -rn --include="*.tsx" --include="*.ts" "#f9a825\|#fefdfb\|#faf3e8\|#fde4b9\|#66bb6a\|primary-50\|primary-100\|primary-200\|primary-300\|primary-400\|primary-500\|primary-600\|primary-700\|primary-800\|primary-900\|surface-50\|surface-100\|surface-200\|accent-500" app/ lib/ 2>/dev/null | grep -v node_modules | wc -l</automated>
    <manual>The automated grep should return 0 lines. If any remain, fix them.</manual>
  </verify>
  <done>Zero occurrences of old orange hex values or old Tailwind token class names in any app/ or lib/ source file. All files import from @/lib/theme/colors where inline style colors are needed. App compiles without errors (npx expo start).</done>
</task>

</tasks>

<verification>
1. `grep -rn "#f9a825\|#fefdfb\|#faf3e8\|#fde4b9\|#66bb6a" app/ lib/` returns zero results
2. `grep -rn "primary-[0-9]\|surface-[0-9]\|accent-5" app/ lib/` returns zero results (excluding comments)
3. tailwind.config.js contains `brand`, `semantic`, `neutral` color objects
4. tailwind.config.js contains 8 fontSize presets
5. tailwind.config.js contains `boxShadow` with DEFAULT and md tiers
6. lib/theme/colors.ts exists and exports `colors` object
</verification>

<success_criteria>
- All 74+ hardcoded orange hex values replaced with new brand/semantic/neutral values
- All 186+ Tailwind class references updated to new token names
- Design token system (colors, typography, elevation) defined in tailwind.config.js
- Color constants exported from lib/theme/colors.ts for inline style usage
- App compiles and renders with green brand palette throughout
</success_criteria>

<output>
After completion, create `.planning/phases/06-design-system-components/06-01-SUMMARY.md`
</output>
