---
phase: 10-onboarding-flow
plan: 04
type: execute
wave: 3
depends_on:
  - 10-03
files_modified:
  - app/(onboarding)/create-household.tsx
  - app/(onboarding)/join-household.tsx
  - app/(onboarding)/member-welcome.tsx
  - app/(onboarding)/module-quiz.tsx
autonomous: true
requirements:
  - ONBD-06
  - ONBD-07

must_haves:
  truths:
    - "Invite code celebration screen shows invite-code.jpg illustration instead of green checkmark"
    - "Invite code is displayed in a dark gradient card with white text and spaced formatting (ABCD EFGH)"
    - "Share with Roommates button opens OS share sheet, Copy code link copies to clipboard"
    - "Continue Setup button advances to module quiz"
    - "Module quiz screen uses Toggle components (from ui/) instead of native Switch for each module"
    - "Module quiz has cream background and StepProgressBar at step 2"
    - "Progress bar is NOT shown on invite code celebration or member-welcome screens"
  artifacts:
    - path: "app/(onboarding)/create-household.tsx"
      provides: "Invite code celebration with dark gradient card, illustration, share/copy actions"
      min_lines: 200
    - path: "app/(onboarding)/module-quiz.tsx"
      provides: "Module quiz with Toggle components, cream bg, progress bar"
      min_lines: 150
    - path: "app/(onboarding)/join-household.tsx"
      provides: "Join household with cream bg, inset input, progress bar"
      min_lines: 100
    - path: "app/(onboarding)/member-welcome.tsx"
      provides: "Member welcome celebration with cream bg, no progress bar"
      min_lines: 80
  key_links:
    - from: "app/(onboarding)/create-household.tsx"
      to: "expo-linear-gradient"
      via: "LinearGradient for dark invite code card"
      pattern: "LinearGradient.*1E293B"
    - from: "app/(onboarding)/create-household.tsx"
      to: "expo-clipboard"
      via: "Clipboard.setStringAsync for copy code"
      pattern: "setStringAsync"
    - from: "app/(onboarding)/module-quiz.tsx"
      to: "components/ui/Toggle.tsx"
      via: "Toggle component for module switches"
      pattern: "Toggle"
    - from: "app/(onboarding)/module-quiz.tsx"
      to: "components/ui/StepProgressBar.tsx"
      via: "StepProgressBar currentStep={2}"
      pattern: "StepProgressBar"
---

<objective>
Restyle the invite code celebration, join-household, member-welcome, and module-quiz screens with dark gradient invite card, Toggle components, illustrations, and cream backgrounds.

Purpose: Completes the onboarding visual redesign with the celebration moment (invite code) and final setup screens (join, welcome, module quiz).
Output: 4 restyled onboarding screen files completing the full onboarding flow redesign
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-onboarding-flow/10-CONTEXT.md
@.planning/phases/10-onboarding-flow/10-RESEARCH.md
@.planning/phases/10-onboarding-flow/10-01-SUMMARY.md
@.planning/phases/10-onboarding-flow/10-03-SUMMARY.md
@app/(onboarding)/create-household.tsx
@app/(onboarding)/join-household.tsx
@app/(onboarding)/member-welcome.tsx
@app/(onboarding)/module-quiz.tsx
@components/ui/Toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle invite code celebration with dark gradient card, illustration, and share/copy actions</name>
  <files>
    app/(onboarding)/create-household.tsx
  </files>
  <action>
    Restyle the invite code SUCCESS STATE in `create-household.tsx` (the conditional view shown after household creation succeeds). The name form was already restyled in Plan 03.

    **Celebration screen layout (replaces the existing invite code success view):**

    1. **Background:** ONBOARDING_CREAM. **No StepProgressBar** on this screen (per CONTEXT.md: "Skip progress bar on invite code celebration screen -- that's a celebration moment, bar feels out of place").

    2. **Illustration hero at top:**
       - Display `ONBOARDING_IMAGES.inviteCode` (invite-code.jpg)
       - ~35-40% height, resizeMode="contain", centered
       - This replaces the existing green checkmark circle

    3. **Title + subtitle:**
       - "You're All Set!" title (large bold, centered)
       - "Share this code with your roommates" subtitle (body, centered)

    4. **Dark gradient invite code card** (mirrors BalanceSummaryCard pattern):
       - LinearGradient colors={['#1E293B', '#0F172A']} start={{x:0,y:0}} end={{x:1,y:1}}
       - borderRadius 16, padding 24, alignItems center
       - "INVITE CODE" overline label in slate-400 (#94A3B8), fontSize 11, fontWeight 600, letterSpacing 1.1, uppercase
       - Invite code in white (#FFFFFF), fontSize 36, fontWeight 700, letterSpacing 4
       - Format code with space for readability: if code is "ABCDEFGH", display as "ABCD EFGH"
       - Use `code.slice(0,4) + ' ' + code.slice(4)` pattern

    5. **Action buttons below the card:**
       - **Primary:** "Share with Roommates" - full-width brand button (bg-brand, white text, rounded-2xl)
         - onPress: use React Native `Share.share()` API (already used in current implementation) to open OS share sheet
         - Share message: "Join my household on RoomY! Use invite code: {code}"
       - **Secondary:** "Copy code" - text link style below share button (text-brand, underline or no underline, centered)
         - onPress: use `expo-clipboard` (`import * as Clipboard from 'expo-clipboard'; await Clipboard.setStringAsync(code)`)
         - Show brief "Copied!" feedback (either via state toggle or a simple text swap)
       - **Tertiary:** "Continue Setup" - outline or secondary button at bottom
         - onPress: navigate to module-quiz (keep existing navigation)

    6. **Remove the old green checkmark circle icon.** The invite-code.jpg illustration replaces it.

    **Preserve ALL existing logic:** household creation, invite code generation, share functionality, error handling. Only change the visual presentation of the success state.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
    <manual>After creating household, see celebration screen with invite-code.jpg, dark gradient card with formatted code, Share/Copy/Continue buttons. No progress bar visible.</manual>
  </verify>
  <done>Invite code celebration shows cream bg, invite-code.jpg hero illustration (replacing checkmark), dark gradient card with formatted invite code (ABCD EFGH), Share with Roommates primary button, Copy code text link with clipboard support, Continue Setup button. No StepProgressBar on this screen.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle join-household, member-welcome, and module-quiz screens</name>
  <files>
    app/(onboarding)/join-household.tsx
    app/(onboarding)/member-welcome.tsx
    app/(onboarding)/module-quiz.tsx
  </files>
  <action>
    **Restyle join-household.tsx:**

    1. Background: ONBOARDING_CREAM
    2. StepProgressBar at top: `<StepProgressBar currentStep={1} totalSteps={3} onBack={() => router.back()} />`
    3. Title: "Join Household" (large bold, centered)
    4. Subtitle: "Enter the invite code from your roommate" (body, centered)
    5. Invite code input: inset style (bg-[#F5F5F5] default, white+brand border on focus), rounded-2xl, autoCapitalize="characters"
    6. "Join" button: full-width brand button
    7. Keep ALL existing RPC call logic, error handling, and navigation
    8. No illustration needed for this screen (it's a simple form entry point, and no illustration was specified in CONTEXT.md for join-household)

    **Restyle member-welcome.tsx:**

    1. Background: ONBOARDING_CREAM
    2. **No StepProgressBar** (this is a celebration screen for joiners, same reasoning as invite code celebration -- skip bar per research recommendation)
    3. Keep the celebratory "You're in!" message
    4. Keep the household name display and member list with colored avatars
    5. Replace any old avatar rendering with Avatar component from components/ui (if not already using it)
    6. "Continue" button: full-width brand button navigating to module-quiz
    7. Keep ALL existing data fetching (household members query) and navigation logic

    **Restyle module-quiz.tsx:**

    1. Background: ONBOARDING_CREAM
    2. StepProgressBar at top: `<StepProgressBar currentStep={2} totalSteps={3} onBack={() => router.back()} />`
    3. Title: "Choose Your Modules" or keep existing title (large bold, centered)
    4. Subtitle: "Pick what you want to manage" (body, centered)
    5. **Replace native Switch components with Toggle component from components/ui:**
       - Import { Toggle } from '@/components/ui'
       - For each module toggle card:
         - Use Card component for the container
         - Module icon (Ionicons) + module name + description on the left
         - Toggle component on the right
         - Expenses toggle should be locked on (use Toggle's `locked` prop if available, or disable interaction)
         - Groceries and Chores toggles are interactive
    6. "Get Started" or "Finish Setup" button: full-width brand button at bottom
    7. Keep ALL existing module settings save logic and navigation (refreshProfile -> redirect to app)

    **No illustration for module-quiz** per research: "Module quiz does not need a hero illustration -- it has 3 toggle cards that take up the content area."
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
    <manual>Join-household shows cream bg, inset input, progress bar. Member-welcome shows cream bg, no progress bar. Module-quiz shows cream bg, Toggle components (not native Switch), progress bar at step 3.</manual>
  </verify>
  <done>Join-household has cream bg, StepProgressBar at step 1, inset code input. Member-welcome has cream bg, no progress bar, Avatar components. Module-quiz has cream bg, StepProgressBar at step 2, Toggle components replacing native Switch, Card containers for each module.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Invite code celebration: dark gradient card, invite-code.jpg, share/copy/continue actions, no progress bar
- Join-household: cream bg, inset input, progress bar step 1
- Member-welcome: cream bg, celebration layout, no progress bar
- Module-quiz: cream bg, Toggle components (not native Switch), progress bar step 2
- All existing Supabase calls, navigation, and business logic preserved
</verification>

<success_criteria>
- Invite code celebration shows dark gradient card with formatted code, illustration hero, share/copy actions
- Join-household, member-welcome, module-quiz all have cream backgrounds
- StepProgressBar appears on join-household (step 1) and module-quiz (step 2), NOT on celebration or member-welcome
- Module quiz uses Toggle components from ui/ instead of native Switch
- All existing onboarding logic (household creation, joining, module save, auth refresh) preserved
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/10-onboarding-flow/10-04-SUMMARY.md`
</output>
