---
phase: 09-groceries-chores
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/chores/StatsRow.tsx
  - components/chores/ChoreRow.tsx
  - components/chores/EmptyState.tsx
  - components/chores/index.ts
  - app/(app)/(tabs)/chores.tsx
autonomous: true
requirements: [CHUI-01, CHUI-02, CHUI-03, CHUI-04]

must_haves:
  truths:
    - "Chores screen shows a stats row with 3 equal-width cards: Pending (warning), Disputed (danger), Streak (brand + fire emoji)"
    - "Chore rows display emoji icons in rounded icon containers mapped by chore type"
    - "Chores screen separates YOUR CHORES from HOUSEHOLD sections with overline headers"
    - "Disputed chore rows use danger-tinted background with red border"
    - "Overdue chores show red tinted background and red due date text"
    - "Streak card shows current streak count and personal best"
  artifacts:
    - path: "components/chores/StatsRow.tsx"
      provides: "3 equal-width stat cards with semantic colors"
      min_lines: 25
    - path: "components/chores/ChoreRow.tsx"
      provides: "Chore row with emoji icon, name, avatar, due date, action buttons"
      min_lines: 40
    - path: "components/chores/EmptyState.tsx"
      provides: "Empty chore list with suggested chores grid"
      min_lines: 20
    - path: "components/chores/index.ts"
      provides: "Barrel export for all chore components"
      min_lines: 3
    - path: "app/(app)/(tabs)/chores.tsx"
      provides: "Parent screen with data fetching, personal best calculation, component composition"
      contains: "calculatePersonalBest"
  key_links:
    - from: "app/(app)/(tabs)/chores.tsx"
      to: "components/chores/StatsRow.tsx"
      via: "import and render with computed counts"
      pattern: "StatsRow.*pendingCount"
    - from: "components/chores/ChoreRow.tsx"
      to: "components/ui/Avatar.tsx"
      via: "Avatar component for assignee display"
      pattern: "Avatar.*userId"
    - from: "components/chores/ChoreRow.tsx"
      to: "getChoreEmoji mapping"
      via: "Emoji text lookup by chore name"
      pattern: "getChoreEmoji"
---

<objective>
Restyle the chores screen with design system components: extract presentational components (StatsRow, ChoreRow, EmptyState), add emoji icons in rounded containers, implement disputed/overdue danger styling, redesign stats row with Card components and semantic colors, add personal best streak calculation.

Purpose: Chores tab gets consistent visual identity with Phase 6 design system -- Card-based stats, Avatar component, emoji chore icons, semantic danger colors for disputes/overdue.
Output: 4 new component files in components/chores/, rewritten chores.tsx parent screen.
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-groceries-chores/09-CONTEXT.md
@.planning/phases/09-groceries-chores/09-RESEARCH.md
@.planning/phases/08-expenses-screen/08-01-SUMMARY.md

@components/ui/Avatar.tsx
@components/ui/Card.tsx
@components/ui/Badge.tsx
@components/ui/index.ts
@lib/theme/colors.ts
@lib/types/database.ts
@app/(app)/(tabs)/chores.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chore presentational components</name>
  <files>
    components/chores/StatsRow.tsx
    components/chores/ChoreRow.tsx
    components/chores/EmptyState.tsx
    components/chores/index.ts
  </files>
  <action>
Create `components/chores/` directory with 3 presentational components and a barrel export, following the Phase 8 `components/expenses/` decomposition pattern.

**StatsRow.tsx** -- 3 equal-width stat cards with semantic colors (CHUI-01):
- Props: `{ pendingCount: number; disputedCount: number; streak: number; personalBest: number }`
- Outer container: `View` with `className="flex-row gap-3 px-4 pt-4 pb-2"`
- Each card: `Card` with `className="flex-1 items-center py-3"`
- Pending card: number in `text-key-number text-semantic-warning`, label "Pending" in `text-metadata text-neutral-secondary`
- Disputed card: number in `text-key-number text-semantic-error`, label "Disputed" in `text-metadata text-neutral-secondary`
- Streak card: streak number in `text-key-number text-brand`, fire emoji as separate `Text` with `style={{ fontSize: 20, marginLeft: 4 }}` in a `flex-row items-center` wrapper. Below: `text-metadata text-neutral-secondary` showing "Best: {personalBest}". When streak is 0 and personalBest is 0, show a dash instead of "0" for streak and "Best: --" for personal best.

**ChoreRow.tsx** -- Emoji icon + chore info + avatar + due date + action buttons (CHUI-02, CHUI-04):
- Props: `{ chore: Chore; assigneeName: string; assigneeId: string | null; isMyChore: boolean; isDisputed: boolean; isDisputedByMe: boolean; overdueDays: number | null; isCompleting: boolean; isClaiming: boolean; isDisputing: boolean; showDisputeButton: boolean; onComplete: () => void; onClaim: () => void; onDispute: () => void; onSwap: () => void }`

- Define `CHORE_EMOJI_MAP` as a module-level constant mapping chore name keywords to emojis:
  ```
  dishes -> plate emoji, trash -> wastebasket emoji, vacuum -> broom emoji,
  clean bathroom / bathroom -> shower emoji, mop -> broom emoji,
  wipe / counters -> sparkles emoji, laundry -> shirt emoji, cook -> cooking emoji
  ```
  Default fallback: clipboard emoji. Export `getChoreEmoji(choreName: string): string` function that does case-insensitive `includes()` matching.

- Emoji icon container: `View` with `h-10 w-10 rounded-xl items-center justify-center mr-3`. Background: `bg-brand-light` normally, `bg-red-100` when disputed. Emoji rendered as `Text` with `style={{ fontSize: 20 }}`.

- Conditional row styling for disputed/overdue (CHUI-04):
  - Disputed: `bg-red-50 border border-red-200 rounded-xl mx-4 my-1`
  - Overdue (not disputed): `bg-red-50/50 rounded-xl mx-4 my-1` with red due date text
  - Normal: `bg-white` (default row styling within the Card)

- Chore info: name in `text-card-title text-neutral-text`, frequency badge in `bg-brand-light px-2 py-0.5 rounded-full` with `text-xs font-medium text-brand-dark`, assignee label ("You" or name) in `text-metadata text-neutral-secondary`, due date in `text-metadata` with color `text-red-600` when overdue otherwise `text-neutral-tertiary`

- Assignee avatar: `Avatar` component with `size="sm"` on the right side, before action buttons

- Action buttons: preserve the existing action button layout (swap/dispute/claim/complete circular buttons) from the current chores.tsx. Keep same Ionicons, colors, disabled states. Keep the "Your completion was disputed" warning text for isDisputedByMe.

**EmptyState.tsx** -- Empty chore list with suggested chores grid:
- Props: `{ onSelectSuggestion: (name: string, frequency: string) => void; onCreateCustom: () => void }`
- Centered icon: `View` with `h-20 w-20 rounded-full bg-brand-light items-center justify-center` and Ionicons "checkbox" size 40
- Heading: `text-section-heading text-neutral-text` "No chores yet!"
- Subtext: `text-body text-neutral-secondary` "Add some chores to keep your home running smoothly"
- Suggested chores grid: same layout as current but use `Card` component for each suggestion card instead of inline shadow styles. Map SUGGESTED_CHORES (keep as module constant with emoji icons instead of Ionicons names):
  ```
  { name: "Dishes", emoji: "plate", frequency: "daily" }
  { name: "Take out trash", emoji: "wastebasket", frequency: "weekly" }
  etc.
  ```
  Each card: `Card` with `className="w-[47%] flex-row items-center px-3 py-3.5"`, emoji in a rounded container `h-8 w-8 rounded-xl bg-brand-light items-center justify-center mr-2.5`, name text.
- "Create custom chore" button: keep existing dashed border button with `text-metadata text-neutral-secondary`
- Move the SUGGESTED_CHORES constant into this file (it's only used here)

**index.ts** -- Barrel export:
```
export { StatsRow } from './StatsRow';
export { ChoreRow, getChoreEmoji } from './ChoreRow';
export { EmptyState } from './EmptyState';
```
  </action>
  <verify>
    <automated>ls components/chores/StatsRow.tsx components/chores/ChoreRow.tsx components/chores/EmptyState.tsx components/chores/index.ts && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>All 4 files exist and TypeScript compiles without errors</manual>
  </verify>
  <done>3 presentational chore components and barrel export exist, TypeScript compiles cleanly, ChoreRow includes emoji mapping and danger styling, StatsRow uses Card components with semantic colors</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite chore parent screen with new components, personal best, and section headers</name>
  <files>
    app/(app)/(tabs)/chores.tsx
  </files>
  <action>
Rewrite `app/(app)/(tabs)/chores.tsx` to use the new presentational components. Preserve ALL existing data fetching, RPC calls, swap modal, dispute logic, and state management exactly as-is. Only change the rendering layer and add personal best calculation.

**Personal best streak calculation (CONTEXT.md locked decision):**
- Add `calculatePersonalBest(completions: ChoreCompletion[]): number` function:
  Iterate through all completions. Track `current` consecutive non-reverted run and `best` max. For each completion: if `is_reverted`, reset `current` to 0; otherwise increment `current` and update `best = Math.max(best, current)`. Return `best`.
- Derive `personalBest` in the derived data section: `const personalBest = calculatePersonalBest(completions);`
- Derive `disputedCount` from `disputedChoreIds.size` (single source of truth per research pitfall 6)

**Section headers (CHUI-03):**
- Replace "My Chores" text with overline: `Text` with `className="text-overline text-neutral-secondary uppercase mb-2 px-4"` showing "YOUR CHORES"
- Replace "Household Chores" text with overline: same styling showing "HOUSEHOLD"
- Wrap each section's chore rows in `Card` with `className="mx-4 p-0 overflow-hidden"` (Phase 8 pattern)

**Stats row:**
- Replace the old 3 plain rounded Views (Pending/Overdue/Streak) with `<StatsRow pendingCount={pendingCount} disputedCount={disputedChoreIds.size} streak={streak} personalBest={personalBest} />`
- Note: change from showing "Overdue" count to "Disputed" count per CONTEXT.md and CHUI-01 requirement

**Chore rows:**
- Replace inline `renderChoreRow()` with `<ChoreRow>` component instances
- Pass all required props from derived state: chore, assigneeName, assigneeId, isMyChore, isDisputed, isDisputedByMe, overdueDays, isCompleting, isClaiming, isDisputing, showDisputeButton
- Pass action callbacks: onComplete, onClaim, onDispute, onSwap
- For disputed rows: ChoreRow handles its own danger-tinted bg/border styling internally -- no wrapper needed in parent
- For normal rows in Card: separate with `border-b border-gray-100` between items (same pattern as groceries/expenses)

**Empty state:**
- Replace inline empty state JSX with `<EmptyState onSelectSuggestion={(name, freq) => router.push({ pathname: '/(app)/chores/add', params: { suggestedName: name, suggestedFrequency: freq } } as never)} onCreateCustom={() => router.push('/(app)/chores/add' as never)} />`

**Swap modal:**
- Replace inline AVATAR_COLORS avatar circles with `Avatar` component from `@/components/ui`:
  `<Avatar userId={member.id} name={member.display_name} size="sm" />`
- Keep existing swap modal structure, Pressable list items, swapSubmitting state

**Completion animation (CONTEXT.md locked decision):**
- In `handleComplete`, after the Alert confirms and RPC succeeds, add a 400ms delay before calling `fetchData()`:
  ```
  if (!error) {
    setTimeout(() => fetchData(), 400);
  }
  ```
  This gives visual feedback before the completed chore disappears from the list.

**Remove dead code:**
- Remove `AVATAR_COLORS` array
- Remove `getInitials()` function
- Remove `SUGGESTED_CHORES` constant (moved to EmptyState component)
- Remove inline `renderChoreRow()` function
- Keep all helper functions: `getOverdueDays`, `getFrequencyLabel`, `formatDueDate`, `calculateStreak`

**Preserve exactly:**
- All useCallback functions (handleComplete, handleClaim, handleDispute, handleSwapRequest)
- fetchData with all its queries (chores, members+profiles, completions, disputed, stale disputes, swap count)
- useFocusEffect refetch pattern
- Swap modal with bottom sheet animation
- All state variables (chores, profiles, completions, disputedChoreIds, disputedByMeChoreIds, pendingSwapCount, completingId, claimingId, disputingId, swapModalChoreId, swapSubmitting)
- Swap requests banner
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30 && npx expo export --platform ios --no-minify 2>&1 | tail -5</automated>
    <manual>Open chores tab in Expo Go: verify 3 stat cards (Pending/Disputed/Streak with personal best), emoji icons on chore rows, YOUR CHORES/HOUSEHOLD section headers, red-tinted disputed rows with borders, overdue red styling, Avatar in swap modal</manual>
  </verify>
  <done>Chores screen renders with 3 Card-based stat cards (Pending warning, Disputed danger, Streak brand with personal best), emoji icons in rounded containers on chore rows, YOUR CHORES/HOUSEHOLD overline section headers, danger-tinted disputed rows with red borders, Avatar component in swap modal replacing old AVATAR_COLORS circles, 400ms completion delay for animation feedback</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx expo export --platform ios --no-minify` builds successfully
- All 4 new component files exist in components/chores/
- chores.tsx imports from components/chores barrel export
- No AVATAR_COLORS or getInitials references remain in chores.tsx
- Avatar component imported from @/components/ui
- Card component used for stat cards and section wrappers
- Disputed rows have bg-red-50 and border-red-200 styling
- Stats row shows Disputed count (not Overdue) matching CONTEXT.md
- personalBest calculation scans all completions for longest consecutive run
</verification>

<success_criteria>
1. Stats row shows 3 equal-width Card-based stat cards: Pending (warning color), Disputed (danger color), Streak (brand color with fire emoji and personal best)
2. Chore rows show emoji icons in rounded containers mapped by chore name
3. "YOUR CHORES" and "HOUSEHOLD" overline section headers separate the two chore lists
4. Disputed chore rows render with red-tinted background and red border
5. Overdue chores show subtle red background tint and red due date text
6. Avatar component replaces all old AVATAR_COLORS circles (rows and swap modal)
7. All existing functionality preserved: complete, claim, dispute, swap requests, stale dispute cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/09-groceries-chores/09-02-SUMMARY.md`
</output>
