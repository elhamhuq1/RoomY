---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - app/(app)/(tabs)/_layout.tsx
  - app/(app)/(tabs)/index.tsx
  - app/(app)/(tabs)/expenses.tsx
  - app/(app)/(tabs)/groceries.tsx
  - app/(app)/(tabs)/chores.tsx
  - app/(app)/settings/index.tsx
  - app/(app)/settings/profile.tsx
  - app/(app)/settings/modules.tsx
  - app/(app)/settings/members.tsx
  - app/(app)/_layout.tsx
autonomous: false
requirements:
  - AUTH-03
  - AUTH-05

must_haves:
  truths:
    - "Dashboard shows only the module tabs the household has enabled"
    - "Empty dashboard for solo creator prominently displays invite code with share action"
    - "User can edit their display name in settings"
    - "User can toggle modules on/off in household settings and tabs update immediately"
    - "User can view household members and share/regenerate invite code in settings"
    - "Household data is isolated -- RLS prevents cross-household access"
  artifacts:
    - path: "app/(app)/(tabs)/_layout.tsx"
      provides: "Dynamic bottom tab navigator showing only enabled modules"
      min_lines: 40
    - path: "app/(app)/(tabs)/index.tsx"
      provides: "Dashboard home with empty state showing invite code"
      min_lines: 60
    - path: "app/(app)/settings/modules.tsx"
      provides: "Module toggle screen accessible post-onboarding"
      min_lines: 50
    - path: "app/(app)/settings/members.tsx"
      provides: "Member list + invite code display + regenerate + share"
      min_lines: 60
  key_links:
    - from: "app/(app)/(tabs)/_layout.tsx"
      to: "lib/auth-context.tsx"
      via: "useSession to read household_settings and conditionally render tabs"
      pattern: "useSession|household_settings|groceries_enabled|chores_enabled"
    - from: "app/(app)/(tabs)/index.tsx"
      to: "lib/auth-context.tsx"
      via: "useSession to check member count for empty state"
      pattern: "useSession"
    - from: "app/(app)/settings/modules.tsx"
      to: "lib/supabase.ts"
      via: "supabase update to household_settings"
      pattern: "supabase\\.from\\('household_settings'\\)"
    - from: "app/(app)/settings/members.tsx"
      to: "lib/supabase.ts"
      via: "supabase query for household members and invite code regeneration"
      pattern: "supabase\\.from\\('household_members'\\)|supabase\\.from\\('households'\\)"
---

<objective>
Build the main app dashboard with dynamic module tabs, empty states for solo creators, and all settings screens (profile, modules, members/invite). This is the home experience after onboarding.

Purpose: Delivers the post-onboarding experience -- dashboard with module tabs that respond to AUTH-05 settings, invite code management from AUTH-03, and validates RLS isolation. After this plan, the foundation phase is complete.

Output: Dashboard with conditional tabs, empty states, profile settings, module toggle settings, and member management with invite code sharing/regeneration.
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dashboard with dynamic tabs and empty states</name>
  <files>
    app/(app)/_layout.tsx
    app/(app)/(tabs)/_layout.tsx
    app/(app)/(tabs)/index.tsx
    app/(app)/(tabs)/expenses.tsx
    app/(app)/(tabs)/groceries.tsx
    app/(app)/(tabs)/chores.tsx
  </files>
  <action>
1. Update `app/(app)/_layout.tsx`:
   - Stack navigator wrapping tabs + settings screens
   - Settings screens accessible via stack push from dashboard

2. Update `app/(app)/(tabs)/_layout.tsx`:
   - Bottom tab navigator using Expo Router Tabs
   - Read household_settings from auth context (useSession)
   - Conditionally show/hide tabs based on enabled modules:
     - "Home" tab: always shown (index.tsx) -- Ionicons `home` icon
     - "Expenses" tab: always shown (expenses is always enabled per user decision) -- Ionicons `wallet` icon
     - "Groceries" tab: only if `groceries_enabled` -- Ionicons `cart` icon
     - "Chores" tab: only if `chores_enabled` -- Ionicons `brush` or `checkbox` icon
   - Settings gear icon in header right -> navigates to settings/index
   - Tab bar styling: warm colors, rounded tab indicator, primary-500 for active tab
   - Use `href={null}` pattern from Expo Router to hide disabled tabs (NOT conditional rendering of Tab.Screen which can cause layout issues)

3. Create `app/(app)/(tabs)/index.tsx` -- Dashboard Home:
   - Fetch household members count from context or Supabase
   - **Solo creator (1 member) empty state** per user decision:
     - "Your household is ready!" header
     - Invite code displayed prominently (large, centered, easily readable)
     - "Share with Roommates" button using native share sheet (same as create-household screen)
     - "Copy Code" button as secondary action
     - Friendly illustration/icon of people joining
     - Per user specific idea: "Empty dashboard should make sharing the invite code the obvious next action -- don't let the creator feel lost alone"
   - **Normal state (2+ members):**
     - Household name as header
     - Member avatars/initials row showing who's in the household
     - Module cards showing enabled features with quick access:
       - Each card is a touchable area linking to the respective tab
       - Cards show module name, icon, and brief status (e.g., "No expenses yet" for Phase 1 placeholders)
     - Module cards appear but show empty/disabled state per user decision: "module cards appear but are empty/disabled until someone joins" (for the transition from 1 to 2+ members)

4. Create placeholder tab screens for Phase 2+ features:
   - `app/(app)/(tabs)/expenses.tsx`: "Expenses coming in Phase 2" placeholder with wallet icon, warm styling
   - `app/(app)/(tabs)/groceries.tsx`: "Grocery list coming in Phase 3" placeholder with cart icon
   - `app/(app)/(tabs)/chores.tsx`: "Chore rotation coming in Phase 3" placeholder with broom icon
   - All placeholders should look intentionally empty (not broken) -- use friendly "coming soon" messaging with appropriate icons
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Test: 1) Solo creator sees invite code prominently. 2) Toggle groceries on in module quiz, verify groceries tab appears. 3) Toggle off, verify tab disappears.</manual>
  </verify>
  <done>
    - Dashboard shows dynamic tabs based on household_settings (groceries/chores conditional)
    - Solo creator empty state: invite code prominent, share button, friendly messaging
    - Normal state: household name, member avatars, module cards
    - Placeholder screens for expenses, groceries, chores with "coming soon" messaging
    - Tab bar uses warm styling with primary-500 active indicator
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build settings screens (profile, modules, members/invite)</name>
  <files>
    app/(app)/settings/index.tsx
    app/(app)/settings/profile.tsx
    app/(app)/settings/modules.tsx
    app/(app)/settings/members.tsx
  </files>
  <action>
1. Create `app/(app)/settings/index.tsx` -- Settings hub:
   - List of settings options as touchable rows:
     - "Profile" -> settings/profile (with user's initials avatar and display name preview)
     - "Modules" -> settings/modules (with count of enabled modules)
     - "Members" -> settings/members (with member count)
     - "Sign Out" -> calls signOut from auth context (with confirmation alert)
   - Clean list layout with chevron icons for navigation rows
   - Header: "Settings" with household name subtitle

2. Create `app/(app)/settings/profile.tsx`:
   - Edit display name (pre-filled with current value)
   - Avatar/initials display (read-only for now -- initials derived from display name)
   - Venmo username field: show but mark as "Used in Phase 2 for payments" or simply include it as an optional field that saves to profiles.venmo_username
   - "Save" button: updates profiles table + auth user metadata
   - Success feedback (brief toast or inline "Saved!" confirmation)
   - Per user decision: "Profile: only display name required during onboarding; Venmo username is optional, prompted when first needed in Phase 2" -- it's fine to show it here in settings as optional

3. Create `app/(app)/settings/modules.tsx`:
   - Per user decision: "Module settings accessible post-onboarding in household settings; changes apply immediately"
   - Same toggle card design as onboarding module-quiz screen (reuse component or duplicate pattern):
     - Expenses: always on, toggle disabled
     - Groceries: toggleable
     - Chores: toggleable
   - Per user decision: "Any household member can toggle modules on/off (not restricted to creator)"
   - On toggle: immediately update household_settings in Supabase + refresh auth context
   - Changes must reflect immediately in the tab bar (groceries/chores tabs appear/disappear)

4. Create `app/(app)/settings/members.tsx`:
   - Per user decision: invite codes expire after 7 days, creator can regenerate anytime
   - Member list:
     - Fetch `household_members` with joined `profiles` data
     - Show each member: initials avatar, display name, role badge (Creator/Member), joined date
   - Invite code section:
     - Display current invite code (large, readable)
     - Show expiry: "Expires [date]" or "Expired" with red indicator
     - "Regenerate Code" button: updates households table with new code + new expiry (now + 7 days). Use `generate_invite_code()` Postgres function or generate client-side.
     - "Share Code" button: native share sheet with pre-written message
     - "Copy Code" button as secondary
   - Household size: "[N]/10 members" indicator (per user decision: reasonable household size limit of 10)
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Test: 1) Open settings, verify all sections navigate correctly. 2) Edit display name, save, verify it updates. 3) Toggle groceries module, go back to dashboard, verify tab changes. 4) View members list. 5) Regenerate invite code.</manual>
  </verify>
  <done>
    - Settings hub with navigation to profile, modules, members, and sign out
    - Profile edit: display name + optional venmo username, saves to Supabase
    - Module toggles: same as onboarding quiz, changes apply immediately to tab bar
    - Members screen: member list with avatars/roles, invite code display, share/copy/regenerate
    - Sign out works and redirects to auth screens
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 flow end-to-end</name>
  <files>None (verification only)</files>
  <action>
Human verification checkpoint. What was built: Complete foundation -- auth (email + social), onboarding wizard (profile -> household -> modules), dashboard with dynamic tabs, settings screens, and Supabase data isolation via RLS.

Steps to verify:
1. Start the app (`npx expo start`) and open on device/emulator
2. Verify welcome screen shows warm/friendly design with value props
3. Sign up with email/password -> verify redirect to profile screen
4. Enter display name -> Continue -> verify equal create/join fork
5. Create a household -> verify invite code is displayed and shareable
6. Continue -> toggle some modules -> Continue -> verify dashboard loads
7. Check that only enabled module tabs are visible in bottom nav
8. Verify solo creator sees invite code prominently on dashboard
9. Open Settings -> edit profile -> save -> verify name updates
10. Open Settings -> Modules -> toggle a module -> go back -> verify tab changes
11. Open Settings -> Members -> verify member list and invite code
12. Sign out -> sign back in -> verify session persistence works
13. (If possible) Sign up a second user, join the household via invite code -> verify member-welcome screen, then dashboard shows 2 members
  </action>
  <verify>Human visual and functional verification of the complete Phase 1 flow</verify>
  <done>User types "approved" to confirm Phase 1 foundation is complete, or describes issues to fix</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Dashboard dynamically shows/hides tabs based on household module settings
- Solo creator empty state prominently shows invite code
- Settings screens all functional (profile edit, module toggle, member list, invite management)
- Module toggle changes in settings immediately reflected in tab bar
- Sign out returns to auth screens
- Full end-to-end flow: welcome -> auth -> onboarding -> dashboard -> settings
</verification>

<success_criteria>
- AUTH-03 complete: invite code visible in dashboard empty state + settings, shareable, regenerable
- AUTH-05 complete: module quiz settings accessible post-onboarding, changes apply immediately
- Dashboard with dynamic module tabs responding to household_settings
- Solo creator experience: invite code is the obvious next action
- All settings screens functional (profile, modules, members)
- End-to-end human verification confirms the complete Phase 1 flow works
- Warm/friendly visual design throughout
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
