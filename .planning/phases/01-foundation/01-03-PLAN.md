---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - app/(onboarding)/_layout.tsx
  - app/(onboarding)/profile.tsx
  - app/(onboarding)/household-choice.tsx
  - app/(onboarding)/create-household.tsx
  - app/(onboarding)/join-household.tsx
  - app/(onboarding)/member-welcome.tsx
  - app/(onboarding)/module-quiz.tsx
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "User can set a display name during onboarding"
    - "User sees an equal fork to create or join a household (neither feels secondary)"
    - "User can create a household and see a shareable invite code"
    - "User can share the invite code via native share sheet"
    - "User can join a household by entering an invite code"
    - "Joining user sees a welcome screen with household name and existing members"
    - "User can toggle grocery and chore modules on/off (expenses always on)"
    - "Completing the onboarding wizard navigates to the dashboard"
  artifacts:
    - path: "app/(onboarding)/profile.tsx"
      provides: "Display name input screen"
      min_lines: 50
    - path: "app/(onboarding)/household-choice.tsx"
      provides: "Create-or-join fork with two equal buttons"
      min_lines: 40
    - path: "app/(onboarding)/create-household.tsx"
      provides: "Household creation form + invite code display + share button"
      min_lines: 80
    - path: "app/(onboarding)/join-household.tsx"
      provides: "Invite code entry form that calls join_household_by_code RPC"
      min_lines: 60
    - path: "app/(onboarding)/member-welcome.tsx"
      provides: "Welcome confirmation showing household name and members"
      min_lines: 50
    - path: "app/(onboarding)/module-quiz.tsx"
      provides: "Toggle cards for groceries and chores modules"
      min_lines: 60
  key_links:
    - from: "app/(onboarding)/profile.tsx"
      to: "lib/supabase.ts"
      via: "supabase update to profiles table"
      pattern: "supabase\\.from\\('profiles'\\)"
    - from: "app/(onboarding)/create-household.tsx"
      to: "lib/supabase.ts"
      via: "supabase insert into households table"
      pattern: "supabase\\.from\\('households'\\)"
    - from: "app/(onboarding)/join-household.tsx"
      to: "lib/supabase.ts"
      via: "supabase.rpc('join_household_by_code')"
      pattern: "supabase\\.rpc\\('join_household_by_code'"
    - from: "app/(onboarding)/module-quiz.tsx"
      to: "lib/supabase.ts"
      via: "supabase update to household_settings table"
      pattern: "supabase\\.from\\('household_settings'\\)"
    - from: "app/(onboarding)/module-quiz.tsx"
      to: "lib/auth-context.tsx"
      via: "refresh household state after completing quiz to trigger navigation to (app) group"
      pattern: "useSession|refreshHousehold"
---

<objective>
Build the complete onboarding wizard: profile setup, household create-or-join fork, invite code sharing, join confirmation, and module quiz with toggle cards. This is the linear wizard flow specified by the user.

Purpose: Delivers AUTH-02 (profile), AUTH-03 (create household + invite code), AUTH-04 (join household via code), and AUTH-05 (module quiz). After this plan, users can form households and configure their app experience.

Output: Seven onboarding screens implementing the full wizard: profile -> household-choice -> (create-household | join-household -> member-welcome) -> module-quiz -> dashboard redirect.
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build profile, household choice, and household creation screens</name>
  <files>
    app/(onboarding)/_layout.tsx
    app/(onboarding)/profile.tsx
    app/(onboarding)/household-choice.tsx
    app/(onboarding)/create-household.tsx
  </files>
  <action>
1. Update `app/(onboarding)/_layout.tsx`:
   - Stack navigator with `headerShown: false`
   - Initial route is "profile"
   - Linear wizard flow: profile -> household-choice -> (create or join branch)

2. Create `app/(onboarding)/profile.tsx`:
   - Display name input (required, per user decision: "only display name required during onboarding")
   - Do NOT ask for Venmo username here (per user decision: "prompted when first needed in Phase 2")
   - Avatar/initials preview: show a circle with the first letter of the display name as they type (per Claude's discretion)
   - On submit: update the `profiles` table via `supabase.from('profiles').update({ display_name }).eq('id', user.id)`
   - Also update auth user metadata: `supabase.auth.updateUser({ data: { display_name } })`
   - Validate: display name must be non-empty, trimmed
   - "Continue" button navigates to household-choice
   - Warm styling: surface-50 background, rounded-xl input, primary-500 button

3. Create `app/(onboarding)/household-choice.tsx`:
   - Per user decision: "Create-or-join fork: after profile setup, a screen with two clear buttons"
   - CRITICAL per user specific idea: "The create-or-join decision point should feel like an equal fork, not 'create is primary, join is secondary'"
   - Two equally-sized, equally-styled cards/buttons:
     - "Create a Household" with a home/house icon -> navigates to create-household
     - "I Have an Invite Code" with a key/link icon -> navigates to join-household
   - Both should be the same visual weight (same size, same border, same prominence)
   - Friendly copy: "How would you like to get started?" or similar
   - Use NativeWind: flex-1 for equal spacing, rounded-2xl cards, consistent padding

4. Create `app/(onboarding)/create-household.tsx`:
   - Input: household name (required)
   - On submit:
     a. Insert into `households` table: `{ name, created_by: user.id, invite_code: generated_server_side }` -- the invite code should be generated by the `generate_invite_code()` Postgres function (use a database default or call the function). Simplest approach: set a Postgres default on the column using `generate_invite_code()`, or generate client-side with a similar approach and set expires_at to now + 7 days.
     b. Insert into `household_members`: `{ household_id, user_id: user.id, role: 'creator' }`
     c. Insert into `household_settings`: `{ household_id, expenses_enabled: true, groceries_enabled: false, chores_enabled: false }`
   - After creation, show the invite code prominently:
     - Large, readable code display (spaced characters, monospace-like font)
     - "Share with Roommates" button using `expo-sharing` or `Share` from react-native (native share sheet with pre-written message: "Join my household on RoomY! Use code: XXXX")
     - Per user decision: "show the invite code on screen AND offer native share button with a pre-written message containing the code"
   - Per user specific idea: "Empty dashboard should make sharing the invite code the obvious next action"
   - "Continue" button navigates to module-quiz
   - Show invite code expiry info: "Code expires in 7 days. You can regenerate it in Settings."
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Navigate through profile -> household-choice. Verify both options look equally prominent. Test household creation and verify invite code displays.</manual>
  </verify>
  <done>
    - Profile screen captures display name, updates profiles table and auth metadata
    - Household choice screen shows two equally-weighted options (create / join) -- neither feels secondary
    - Create household screen: creates household + membership + settings, displays invite code, offers native share sheet
    - Wizard flows linearly: profile -> household-choice -> create-household
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build join household, member welcome, and module quiz screens</name>
  <files>
    app/(onboarding)/join-household.tsx
    app/(onboarding)/member-welcome.tsx
    app/(onboarding)/module-quiz.tsx
  </files>
  <action>
1. Create `app/(onboarding)/join-household.tsx`:
   - Invite code input: text field for 8-character code (uppercase, per generate_invite_code format)
   - Auto-capitalize input, filter to allowed characters
   - On submit: call `supabase.rpc('join_household_by_code', { code: inputCode })`
   - Handle errors from the RPC function:
     - "Invalid or expired invite code" -> show friendly error: "This code is invalid or has expired. Ask your roommate for a new one."
     - "Household is full" -> "This household has reached its member limit."
     - "Already a member of this household" -> "You're already in this household!"
   - On success: navigate to member-welcome, passing household data from RPC response (household_id, household_name, member_count)
   - Loading state while joining
   - Back button to household-choice

2. Create `app/(onboarding)/member-welcome.tsx`:
   - Per user decision: "When a new member joins: household welcome screen showing household name, existing members' names/avatars, and 'You're in!' confirmation"
   - Receive household data from navigation params or re-fetch from Supabase
   - Display:
     - "You're in!" header with celebratory feel (friendly, not corporate)
     - Household name prominently shown
     - List of existing members with initials/avatar circles and display names
     - Member count: "You're member #N of [household name]"
   - Fetch member list: `supabase.from('household_members').select('*, profiles(*)').eq('household_id', householdId)`
   - "Continue" button navigates to module-quiz
   - Warm, celebratory styling -- this is a positive moment in the onboarding

3. Create `app/(onboarding)/module-quiz.tsx`:
   - Per user decision: "Toggle cards on one screen: one card per module with icon, short description, and toggle switch"
   - Three module cards:
     a. **Expenses** (always on, toggle disabled/locked): wallet icon, "Split bills and track who owes what" description, toggle is ON and non-interactive with subtle "Always enabled" label
     b. **Groceries** (opt-in, default off): cart icon, "Shared shopping list with cost splitting" description, toggle switch
     c. **Chores** (opt-in, default off): broom/clean icon, "Fair chore rotation with effort tracking" description, toggle switch
   - Per user decision: "Expenses is always on and cannot be disabled (core value of the app); groceries and chores are opt-in"
   - Per user decision: "Any household member can toggle modules on/off (not restricted to creator)"
   - On "Continue":
     a. Update `household_settings` via `supabase.from('household_settings').update({ groceries_enabled, chores_enabled }).eq('household_id', householdId)`
     b. Refresh the auth context household state (call a refresh function on useSession or re-fetch) so that `hasCompletedOnboarding` becomes true
     c. Stack.Protected guard will then redirect to `(app)` group automatically
   - CRITICAL: After saving settings and refreshing auth context, the user must land on the dashboard. The auth-context `household` state being non-null triggers the `(app)` group's guard.
   - Cards should use NativeWind: rounded-2xl, shadow, padding, with icon on left and toggle on right

Ensure the auth-context `refreshHousehold` or equivalent function exists. If 01-01 didn't add it, add a method to AuthContext that re-fetches profile and household membership. Call it after household creation/joining and after module quiz save.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Test join flow: enter a valid invite code -> see member-welcome with household info -> continue to module quiz -> toggle modules -> continue to dashboard. Test error states with invalid codes.</manual>
  </verify>
  <done>
    - Join household screen: invite code input, calls join_household_by_code RPC, handles all error cases with friendly messages
    - Member welcome screen: shows "You're in!", household name, member list with avatars/initials
    - Module quiz screen: three toggle cards (Expenses locked on, Groceries/Chores toggleable), saves to household_settings
    - Completing module quiz refreshes auth context and navigates to dashboard (via Stack.Protected)
    - Full wizard flow works: profile -> household-choice -> (create|join) -> module-quiz -> dashboard
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Complete onboarding wizard: profile -> household-choice -> create-household -> module-quiz -> dashboard
- Alternate path: profile -> household-choice -> join-household -> member-welcome -> module-quiz -> dashboard
- Household creation inserts correct rows in households, household_members, household_settings
- Join via invite code works through Supabase RPC
- Invalid/expired codes show friendly error messages
- Module quiz saves settings and redirects to main app
- Invite code is shareable via native share sheet
- Both create and join options look equally prominent on household-choice screen
</verification>

<success_criteria>
- Full linear onboarding wizard per user decision (sign up -> profile -> create/join -> quiz -> dashboard)
- AUTH-02: Profile with display name created during onboarding
- AUTH-03: Household created with invite code, shareable via share sheet + visible on screen
- AUTH-04: Household joining via invite code with validation and welcome screen
- AUTH-05: Module quiz with toggle cards, expenses always on, groceries/chores opt-in
- Equal fork at household-choice (per user specific idea)
- Warm/friendly visual design throughout
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
