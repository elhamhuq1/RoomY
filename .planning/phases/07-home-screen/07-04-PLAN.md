---
phase: 07-home-screen
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/calendar-utils.ts
  - app/(app)/(tabs)/index.tsx
  - components/home/BalanceSummaryCard.tsx
  - components/home/WeeklyTimeline.tsx
autonomous: true
requirements: [HOME-04, HOME-06]
gap_closure: true

must_haves:
  truths:
    - "WeeklyTimeline shows chores for the current week even when chore next_due_at falls outside the week (recurring projection)"
    - "Balance card hides Settle Up and Request buttons when balance is zero (All settled up)"
    - "Pull-to-refresh preserves the selected calendar date and scopes data to the selected month/week"
  artifacts:
    - path: "lib/calendar-utils.ts"
      provides: "Exported projectChoreDates function for reuse outside buildMarkedDates"
      contains: "export function projectChoreDates"
    - path: "app/(app)/(tabs)/index.tsx"
      provides: "Fixed weekChores using projectChoreDates, selectedDate-aware fetchAllData"
      contains: "projectChoreDates"
    - path: "components/home/BalanceSummaryCard.tsx"
      provides: "Conditionally hidden buttons when settled"
      contains: "!isSettled"
    - path: "components/home/WeeklyTimeline.tsx"
      provides: "Simplified component without redundant week filtering"
  key_links:
    - from: "app/(app)/(tabs)/index.tsx"
      to: "lib/calendar-utils.ts"
      via: "import { projectChoreDates }"
      pattern: "projectChoreDates"
    - from: "app/(app)/(tabs)/index.tsx"
      to: "components/home/WeeklyTimeline.tsx"
      via: "weekChores prop now contains projected recurring dates"
      pattern: "weekChores"
    - from: "app/(app)/(tabs)/index.tsx"
      to: "components/home/BalanceSummaryCard.tsx"
      via: "onSettleUp and onRequest no longer callable when settled"
      pattern: "BalanceSummaryCard"
---

<objective>
Fix 3 UAT gaps from Phase 7 user testing: empty WeeklyTimeline (major), visible balance buttons when settled (minor), and pull-to-refresh resetting date context (minor).

Purpose: These 3 bugs were diagnosed during UAT. The WeeklyTimeline shows nothing because weekChores filters by a single next_due_at timestamp instead of projecting recurring dates. The balance buttons remain tappable at zero balance causing infinite loading. Pull-to-refresh scopes data to current month/week instead of respecting the selected date.

Output: All 3 fixes applied to existing files, WeeklyTimeline shows recurring chores, balance buttons hidden when settled, refresh preserves date context.
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-home-screen/07-03-SUMMARY.md
@.planning/phases/07-home-screen/07-UAT.md
@.planning/debug/weekly-timeline-empty.md
@.planning/debug/balance-buttons-when-settled.md
@.planning/debug/pull-refresh-resets-date.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WeeklyTimeline data pipeline and export projectChoreDates</name>
  <files>lib/calendar-utils.ts, app/(app)/(tabs)/index.tsx, components/home/WeeklyTimeline.tsx</files>
  <action>
Three changes across three files to fix the empty WeeklyTimeline:

**1. Export projectChoreDates in lib/calendar-utils.ts (line 52)**

Change `function projectChoreDates(chore: Chore, monthDate: Date): string[]` to `export function projectChoreDates(chore: Chore, monthDate: Date): string[]`. This is the only change in this file -- add the `export` keyword. The function already works correctly (CalendarSection uses it via buildMarkedDates and it shows dots fine). We just need direct access from index.tsx.

**2. Rewrite weekChores useMemo in app/(app)/(tabs)/index.tsx (lines 240-263)**

Replace the current weekChores useMemo with one that uses projectChoreDates to project recurring chore dates within the selected week (not just checking single next_due_at).

New logic:
- Import `projectChoreDates` from `@/lib/calendar-utils` (add to existing import if there is one, or add new import).
- Compute week boundaries from `selectedDate` (parsed via parseISO) instead of `new Date()`. Use `startOfWeek(parseISO(selectedDate), { weekStartsOn: 0 })` and `endOfWeek(parseISO(selectedDate), { weekStartsOn: 0 })`.
- For each chore in `chores`, call `projectChoreDates(chore, parseISO(selectedDate))` to get all projected date strings for that month.
- Filter the projected dates to only those within the week interval (wStart to wEnd). Use `isWithinInterval(parseISO(dateStr), { start: wStart, end: wEnd })`.
- For each matching projected date, create a timeline entry with: id (use `${chore.id}-${dateStr}` to create unique keys per occurrence), name, assigneeId, assigneeName (looked up from members), dueDate (the projected dateStr), isCompleted (check if chore.last_completed_at exists AND isSameDay(parseISO(chore.last_completed_at), parseISO(dateStr))).
- Flatten the result so multiple occurrences of the same chore on different days each get their own entry.
- Add `selectedDate` to the useMemo dependency array: `[chores, members, selectedDate]`.

**3. Remove redundant week-filtering in components/home/WeeklyTimeline.tsx (lines 36-71)**

The WeeklyTimeline component currently has its own redundant week-filtering logic inside the `dayGroups` useMemo (lines 42-48: filtering chores by isWithinInterval for current week, or by selectedDate). Since the upstream weekChores is now correctly pre-filtered and date-projected, simplify this:

- Remove the `now`, `weekStart`, `weekEnd` variables (lines 37-39).
- Remove the `filtered` block (lines 42-48) -- the incoming `chores` prop is already correctly filtered.
- Use `chores` directly instead of `filtered` in the grouping logic (line 52 onwards).
- Keep the selectedDate single-day filter: if `selectedDate` is non-null, filter chores to only those where `isSameDay(parseISO(chore.dueDate), parseISO(selectedDate))`. Otherwise use all chores (full week).
- Remove unused imports: `startOfWeek`, `endOfWeek`, `isWithinInterval` from date-fns (keep `format`, `parseISO`, `isSameDay`).
- Keep all rendering logic unchanged.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit 2>&1 | grep -E "calendar-utils|index\.tsx|WeeklyTimeline" | head -20; echo "Exit: $?"</automated>
    <manual>On device: open home screen, verify WeeklyTimeline section shows chores for the current week with day headers, assignee avatars, and completion indicators. Select a different day in the calendar and verify the timeline filters to that day only.</manual>
  </verify>
  <done>WeeklyTimeline displays recurring chores projected from frequency, not just single next_due_at. Selecting a date in the calendar correctly filters the timeline. No TypeScript errors in modified files.</done>
</task>

<task type="auto">
  <name>Task 2: Hide balance buttons when settled and make refresh date-aware</name>
  <files>components/home/BalanceSummaryCard.tsx, app/(app)/(tabs)/index.tsx</files>
  <action>
Two targeted fixes:

**1. Hide buttons when settled in components/home/BalanceSummaryCard.tsx**

Replace lines 72-89 (the action buttons View) with a conditional render. Wrap the entire buttons View in `{!isSettled && (...)}` so the Settle Up and Request buttons are completely hidden (not just dimmed) when the balance is zero.

Specifically, change:
```
{/* Action buttons */}
<View
  className="mt-4 flex-row gap-3"
  style={isSettled ? { opacity: 0.7 } : undefined}
>
  <Button ... />
  <Button ... />
</View>
```

To:
```
{/* Action buttons - hidden when settled */}
{!isSettled && (
  <View className="mt-4 flex-row gap-3">
    <Button ... />
    <Button ... />
  </View>
)}
```

Remove the `style={isSettled ? { opacity: 0.7 } : undefined}` since it's no longer needed. Keep all Button props unchanged.

**2. Make fetchAllData and weekChores respect selectedDate in app/(app)/(tabs)/index.tsx**

(a) Add `selectedDate` to fetchAllData's dependency and scope:
- Change the `fetchAllData` useCallback to accept and use selectedDate for expense scoping.
- Replace `const monthStart = startOfMonth(new Date()).toISOString()` with `const monthStart = startOfMonth(parseISO(selectedDate)).toISOString()`.
- Replace `const monthEnd = endOfMonth(new Date()).toISOString()` with `const monthEnd = endOfMonth(parseISO(selectedDate)).toISOString()`.
- Add `selectedDate` to the useCallback dependency array: `[household?.id, selectedDate]`.

(b) Since fetchAllData now depends on selectedDate, the useFocusEffect and onRefresh will automatically use the current selectedDate. No changes needed to onRefresh itself.

(c) The weekChores useMemo was already updated in Task 1 to use selectedDate instead of new Date() for week boundaries. Verify this is consistent.

(d) Remove the `todayString` and `isDateFiltered` variables (lines 266-267) since they are no longer needed -- the WeeklyTimeline already receives correctly scoped chores. Update the WeeklyTimeline JSX to pass `selectedDate={null}` always (or remove the selectedDate prop entirely since the parent now pre-filters). Actually, keep passing `selectedDate` for single-day filtering in the timeline: when the user taps a specific day in the calendar, the timeline should show only that day. The logic is: if selectedDate is not today, pass it; if today, pass null (show full week). Keep the existing `isDateFiltered` pattern for this purpose -- retain `todayString` and `isDateFiltered` for the WeeklyTimeline selectedDate prop only.

Correction on (d): Keep `todayString` and `isDateFiltered` as-is. They serve the WeeklyTimeline single-day filter purpose. The key fix is only (a): making fetchAllData scope expenses to the selected month.
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit 2>&1 | grep -E "BalanceSummaryCard|index\.tsx" | head -20; echo "Exit: $?"</automated>
    <manual>On device: (1) Set all balances to zero (settle up), verify Settle Up and Request buttons are completely hidden on the balance card. (2) Select a different week in the calendar, pull-to-refresh, verify the calendar stays on the selected week and expense dots remain for the selected month.</manual>
  </verify>
  <done>Balance card shows no action buttons when "All settled up". Pull-to-refresh preserves selected date context by scoping expense fetch to the selected month. No TypeScript errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` passes with no new errors in Phase 7 files
2. App loads on device without crashes
3. WeeklyTimeline shows recurring chores with correct day grouping
4. Balance card buttons hidden when zero balance
5. Pull-to-refresh preserves selected date and data context
</verification>

<success_criteria>
All 3 UAT gaps closed:
- WeeklyTimeline renders chore entries using frequency-based date projection (no empty state when chores exist)
- Balance card hides Settle Up / Request when net balance is zero
- Pull-to-refresh scopes expense data to the selected month and weekChores to the selected week
</success_criteria>

<output>
After completion, create `.planning/phases/07-home-screen/07-04-SUMMARY.md`
</output>
