---
phase: 07-home-screen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/calendar-utils.ts
  - components/home/CalendarSection.tsx
autonomous: true
requirements: [HOME-03]

must_haves:
  truths:
    - "Week-strip calendar shows 7 days with today highlighted and event dots"
    - "Calendar expands to full month view on tap and collapses back"
    - "Event dots use green for chores and red/coral for expenses"
    - "Tapping a day updates the selected date for filtering downstream"
  artifacts:
    - path: "components/home/CalendarSection.tsx"
      provides: "Collapsible week-strip calendar with event dots"
      min_lines: 40
    - path: "lib/calendar-utils.ts"
      provides: "Updated dot colors (red/coral for expenses, green for chores)"
      contains: "EF4444"
  key_links:
    - from: "components/home/CalendarSection.tsx"
      to: "react-native-calendars"
      via: "ExpandableCalendar or Calendar with week toggle"
      pattern: "ExpandableCalendar|Calendar"
    - from: "components/home/CalendarSection.tsx"
      to: "lib/calendar-utils.ts"
      via: "buildMarkedDates for dot markers"
      pattern: "buildMarkedDates"
    - from: "lib/calendar-utils.ts"
      to: "colors"
      via: "Updated EXPENSE_DOT color from blue to red/coral"
      pattern: "EXPENSE_DOT.*EF4444|F87171"
---

<objective>
Create the collapsible week-strip calendar component and update calendar dot colors to match the new design.

Purpose: The calendar provides an at-a-glance view of upcoming events (chores and expenses) with the ability to drill into specific days. Dot colors are changing from blue/green to red/green per the design spec.
Output: CalendarSection component and updated calendar-utils.ts with new dot colors.
</objective>

<execution_context>
@/home/elham/.claude/get-shit-done/workflows/execute-plan.md
@/home/elham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-home-screen/07-CONTEXT.md
@.planning/phases/07-home-screen/07-RESEARCH.md
@lib/calendar-utils.ts
@lib/theme/colors.ts
@app/(app)/(tabs)/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update calendar-utils dot colors and create CalendarSection component</name>
  <files>lib/calendar-utils.ts, components/home/CalendarSection.tsx</files>
  <action>
**Update `lib/calendar-utils.ts`:**
- Change `EXPENSE_DOT` color from `#3b82f6` (blue) to `#EF4444` (red/coral) to match the new design spec
- Keep `CHORE_DOT` color as `#22c55e` (green) -- unchanged
- Update the `getEventsForDate` function: change expense event `color` from `#3b82f6` to `#EF4444`
- Export `EXPENSE_DOT` and `CHORE_DOT` constants so CalendarSection can use them for its legend

**Create `components/home/CalendarSection.tsx`:**
- Accept props: `{ expenses: Expense[]; chores: Chore[]; selectedDate: string; onDateChange: (date: string) => void }`
- The component wraps the calendar interaction: week-strip that expands to full month on tap.

**Implementation approach -- try ExpandableCalendar first, fall back if needed:**

Primary approach (ExpandableCalendar):
- Import `CalendarProvider` and `ExpandableCalendar` from `react-native-calendars`
- Wrap in `CalendarProvider` with `date={selectedDate}` and `onDateChanged={onDateChange}`
- Use `ExpandableCalendar` with:
  - `initialPosition={ExpandableCalendar.positions.CLOSED}` (starts as week strip)
  - `markingType="multi-dot"`
  - `markedDates` computed via `buildMarkedDates(expenses, chores, currentMonth)` with useMemo
  - `closeOnDayPress={false}` (tapping a day selects it, doesn't collapse)
  - Theme: `calendarBackground: '#ffffff'`, `todayTextColor: colors.brand.DEFAULT`, `selectedDayBackgroundColor: colors.brand.DEFAULT`, `selectedDayTextColor: '#ffffff'`, `arrowColor: colors.brand.DEFAULT`
- Track `currentMonth` state internally for `buildMarkedDates` calculation
- Include a color legend below the calendar: two rows with colored dots and labels ("Chores" for green, "Expenses" for red)

IMPORTANT: ExpandableCalendar may have gesture conflicts inside a parent ScrollView (see Pitfall 2 in research). If ExpandableCalendar proves unworkable during execution, use this fallback:

Fallback approach (custom toggle with existing Calendar):
- Use a boolean `expanded` state
- When collapsed, render a custom week strip: 7-day row showing Sun-Sat for the current week with:
  - Day name abbreviation (S, M, T, W, T, F, S) as `text-metadata text-neutral-secondary`
  - Date number below, highlighted with `bg-brand rounded-full text-white` if today, `bg-neutral-surface rounded-full` if selected
  - Small dot indicators below each date (green/red) from markedDates
  - Tap any day calls `onDateChange(dateString)`
  - Tap a "month" button or chevron toggles `expanded = true`
- When expanded, render the existing `Calendar` component from `react-native-calendars` with `markingType="multi-dot"` and standard theme
  - Use `LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut)` before toggling `expanded` for smooth transition
  - Tap a day or tap collapse icon sets `expanded = false`

In either approach:
- Compute `markedDates` using `buildMarkedDates` from calendar-utils with useMemo, adding selected date highlight
- Color legend at bottom with dot-label pairs using the exported dot color constants
  </action>
  <verify>
    <automated>cd /home/elham/projects/sb-proj && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>CalendarSection renders a week strip or expandable calendar. Expense dots are now red/coral instead of blue.</manual>
  </verify>
  <done>calendar-utils.ts uses red/coral (#EF4444) for expense dots instead of blue. CalendarSection component renders a collapsible week-strip calendar with multi-dot markers and selected date callback. Color legend shows correct dot-color associations.</done>
</task>

</tasks>

<verification>
- `lib/calendar-utils.ts` EXPENSE_DOT color is `#EF4444` (not `#3b82f6`)
- `lib/calendar-utils.ts` getEventsForDate expense color is `#EF4444`
- `components/home/CalendarSection.tsx` exists and compiles
- CalendarSection uses `buildMarkedDates` from calendar-utils
- CalendarSection accepts expenses, chores, selectedDate, and onDateChange props
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Calendar dot colors updated: red/coral for expenses, green for chores
- CalendarSection shows 7-day week strip by default
- Calendar can expand to full month view and collapse back
- Tapping a day triggers onDateChange callback
- Today is visually highlighted with brand color
- Event dots appear on days with chores/expenses
- Color legend renders below calendar
</success_criteria>

<output>
After completion, create `.planning/phases/07-home-screen/07-02-SUMMARY.md`
</output>
